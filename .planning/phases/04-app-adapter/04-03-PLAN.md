---
phase: 04-app-adapter
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - config.yaml
  - scripts/download_drama.py
  - tests/test_download_drama.py
autonomous: true
requirements:
  - ADAPTER-03

must_haves:
  truths:
    - 用户可以通过配置文件选择目标 App（如 app: honguo）
    - download_drama.py 使用 adapter 架构而非硬编码红果逻辑
    - 红果 App 的所有现有功能在新架构下正常工作（向后兼容）
  artifacts:
    - path: "config.yaml"
      provides: "App 配置文件模板"
      min_lines: 15
      contains: "app: honguo"
    - path: "scripts/download_drama.py"
      provides: "集成 AppAdapter 的主流程"
      exports: ["main"]
  key_links:
    - from: "main()"
      to: "create_adapter(config['app'])"
      via: "从配置文件加载 adapter"
      pattern: "adapter = create_adapter\\(.*\\)"
    - from: "download_and_decrypt"
      to: "adapter.parse_ui_context"
      via: "使用 adapter 解析 UI"
      pattern: "ui_ctx = adapter\\.parse_ui_context\\(xml\\)"
    - from: "_try_start_episode_on_drama_page"
      to: "adapter.select_episode"
      via: "使用 adapter 选集"
      pattern: "adapter\\.select_episode\\(ep_num\\)"
---

<objective>
将 AppAdapter 集成到 download_drama.py 主流程，实现配置化的 App 选择。

**Purpose**: 替换 download_drama.py 中硬编码的红果 App 逻辑，使用 adapter 架构。用户可以通过配置文件选择目标 App，为未来支持快手、抖音等平台做好准备。

**Output**: 
- config.yaml 配置文件模板
- download_drama.py 集成 adapter 架构
- 向后兼容：默认使用红果 adapter，现有功能不受影响
- 更新测试以验证 adapter 集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-app-adapter/04-CONTEXT.md
@.planning/phases/04-app-adapter/04-01-PLAN.md
@.planning/phases/04-app-adapter/04-02-PLAN.md
@scripts/app_adapter.py
@scripts/download_drama.py
</context>

<interfaces>
<!-- AppAdapter 接口（从 Plan 01-02） -->

从 scripts/app_adapter.py：
```python
def create_adapter(app_name: str) -> AppAdapter:
    """工厂函数：创建 adapter 实例"""
    pass

class HongGuoAdapter(AppAdapter):
    def get_package_name(self) -> str:
        return "com.phoenix.read"
    
    def get_hook_script(self) -> str:
        return "frida_hooks/ttengine_all.js"
    
    def parse_ui_context(self, xml: str, **kwargs) -> UIContext:
        pass
    
    def select_episode(self, ep_num: int, **kwargs) -> bool:
        pass
```

<!-- 现有 download_drama.py 关键函数 -->

从 scripts/download_drama.py：
```python
APP_PACKAGE = "com.phoenix.read"  # 需要替换为 adapter.get_package_name()

def main():
    """主入口函数"""
    pass

def download_and_decrypt(...):
    """下载和解密单集"""
    # 使用 parse_ui_context(xml) → 改为 adapter.parse_ui_context(xml)
    pass

def _try_start_episode_on_drama_page(ep_num: int, state: Optional[CaptureState] = None) -> bool:
    """在剧集页面启动指定集数"""
    # 使用 select_episode_from_ui(ep_num) → 改为 adapter.select_episode(ep_num)
    pass

def search_drama_in_app(name: str, start_episode: int = 1, state: Optional[CaptureState] = None) -> bool:
    """在 App 内搜索剧名"""
    # 使用 select_episode_from_ui(ep_num) → 改为 adapter.select_episode(ep_num)
    pass
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建配置文件模板</name>
  <files>config.yaml</files>
  <action>
创建 `config.yaml` 配置文件，按照 04-CONTEXT.md 决策 2 的格式：

**文件内容**：
```yaml
# 红果短剧下载器配置文件
# 用于选择目标 App 和自定义行为

# 当前使用的 App（可选值：honguo, kuaishou, douyin）
app: honguo

# App 特定配置
apps:
  honguo:
    package: com.phoenix.read
    hook_script: frida_hooks/ttengine_all.js
    # 可选：自定义 UI 元素定位符（未来扩展）
    # ui_selectors:
    #   title_resource_id: com.phoenix.read:id/d4
    #   episode_resource_id: com.phoenix.read:id/jjj
  
  # 未来支持的 App（示例）
  # kuaishou:
  #   package: com.kuaishou.nebula
  #   hook_script: frida_hooks/kuaishou_hook.js
  
  # douyin:
  #   package: com.ss.android.ugc.aweme
  #   hook_script: frida_hooks/douyin_hook.js

# 下载配置
download:
  # 默认画质（可选值：720p, 1080p, origin）
  quality: 720p
  
  # 最大重试次数
  max_retries: 3
  
  # 输出目录
  output_dir: videos

# Frida 配置
frida:
  # Frida 版本（必须与设备端 frida-server 匹配）
  version: 16.5.9
  
  # 连接超时（秒）
  timeout: 10
```

**设计要点**：
- 使用 YAML 格式（易读易写）
- 顶层 `app` 字段指定当前使用的 App
- `apps` 字段包含所有支持的 App 配置
- 添加注释说明每个字段的用途
- 预留未来扩展字段（如 ui_selectors）
  </action>
  <verify>
    <automated>python -c "import yaml; config = yaml.safe_load(open('config.yaml')); assert config['app'] == 'honguo'; print('Config loaded:', config['app'])"</automated>
  </verify>
  <done>
- config.yaml 文件存在且格式正确
- 包含 app 字段和 apps 配置
- 可以被 PyYAML 正确解析
- 默认配置为 honguo
  </done>
</task>

<task type="auto">
  <name>Task 2: 集成 adapter 到 download_drama.py</name>
  <files>scripts/download_drama.py</files>
  <action>
修改 `scripts/download_drama.py`，集成 AppAdapter 架构：

**步骤 1：添加配置加载**

在文件顶部添加导入：
```python
import yaml
from pathlib import Path
from scripts.app_adapter import create_adapter, AppAdapter
```

在模块级别添加配置加载函数：
```python
def load_config(config_path: str = "config.yaml") -> dict:
    """加载配置文件
    
    Args:
        config_path: 配置文件路径
    
    Returns:
        配置字典
    
    Raises:
        FileNotFoundError: 配置文件不存在
        yaml.YAMLError: 配置文件格式错误
    """
    path = Path(config_path)
    if not path.exists():
        logger.warning(f"配置文件 {config_path} 不存在，使用默认配置")
        return {"app": "honguo"}  # 默认配置（向后兼容）
    
    with open(path, 'r', encoding='utf-8') as f:
        return yaml.safe_load(f)
```

**步骤 2：在 main() 中创建 adapter**

在 `main()` 函数开头添加：
```python
def main():
    # 加载配置并创建 adapter
    config = load_config()
    app_name = config.get('app', 'honguo')
    adapter = create_adapter(app_name)
    logger.info(f"使用 {app_name} adapter (包名: {adapter.get_package_name()})")
    
    # 原有的参数解析逻辑...
    parser = argparse.ArgumentParser(...)
```

将 adapter 传递给需要的函数（通过参数或全局变量）。

**步骤 3：替换硬编码的 APP_PACKAGE**

找到所有使用 `APP_PACKAGE` 的地方，替换为 `adapter.get_package_name()`：
- Frida 注入时的包名参数
- ADB 命令中的包名

**步骤 4：替换 parse_ui_context 调用**

找到所有 `parse_ui_context(xml)` 调用，替换为 `adapter.parse_ui_context(xml)`：
- 在 `download_and_decrypt` 函数中
- 在 `_try_start_episode_on_drama_page` 函数中
- 在其他 UI 解析位置

**步骤 5：替换 select_episode_from_ui 调用**

找到所有 `select_episode_from_ui(ep_num)` 调用，替换为 `adapter.select_episode(ep_num)`：
- 在 `_try_start_episode_on_drama_page` 函数中
- 在 `search_drama_in_app` 函数中

**步骤 6：处理 adapter 传递**

由于多个函数需要使用 adapter，有两种方案：
- 方案 A：将 adapter 作为参数传递给需要的函数
- 方案 B：使用模块级全局变量（类似 CaptureState 的单例模式）

推荐方案 B（与现有 CaptureState 模式一致）：
```python
_adapter_instance: Optional[AppAdapter] = None

def get_adapter() -> AppAdapter:
    """获取全局 adapter 实例"""
    if _adapter_instance is None:
        raise RuntimeError("Adapter not initialized. Call set_adapter() first.")
    return _adapter_instance

def set_adapter(adapter: AppAdapter):
    """设置全局 adapter 实例"""
    global _adapter_instance
    _adapter_instance = adapter
```

在 `main()` 中调用 `set_adapter(adapter)`，其他函数通过 `get_adapter()` 访问。

**注意事项**：
- 保持向后兼容：如果 config.yaml 不存在，默认使用 honguo
- 不要删除 `APP_PACKAGE` 常量（可能被测试使用），但在代码中替换为 adapter 调用
- 确保所有 Frida Hook 注入使用 adapter.get_package_name()
  </action>
  <verify>
    <automated>python -c "from scripts.download_drama import load_config, get_adapter, set_adapter; from scripts.app_adapter import create_adapter; config = load_config(); adapter = create_adapter(config['app']); set_adapter(adapter); assert get_adapter().get_package_name() == 'com.phoenix.read'"</automated>
  </verify>
  <done>
- load_config 函数实现完成
- main() 中创建并设置 adapter
- 所有 APP_PACKAGE 使用替换为 adapter.get_package_name()
- 所有 parse_ui_context 调用替换为 adapter.parse_ui_context()
- 所有 select_episode_from_ui 调用替换为 adapter.select_episode()
- 代码可以正常导入且无语法错误
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新测试以验证 adapter 集成</name>
  <files>tests/test_download_drama.py</files>
  <action>
更新 `tests/test_download_drama.py`，验证 adapter 集成：

**新增测试用例**：

1. `test_load_config_success`
   - 创建临时 config.yaml 文件
   - 调用 `load_config(temp_path)`
   - 验证返回的配置字典包含 `app` 字段

2. `test_load_config_missing_file`
   - 调用 `load_config("nonexistent.yaml")`
   - 验证返回默认配置 `{"app": "honguo"}`（向后兼容）

3. `test_adapter_integration_in_main`
   - 使用 `unittest.mock.patch` 模拟 Frida 和 ADB 调用
   - 模拟 `create_adapter` 返回 mock adapter
   - 调用 `main()` 的部分逻辑（adapter 初始化部分）
   - 验证 `set_adapter` 被调用
   - 验证 `get_adapter()` 返回正确的 adapter

4. `test_adapter_used_in_parse_ui`
   - 模拟 `get_adapter().parse_ui_context` 返回 mock UIContext
   - 调用使用 UI 解析的函数（如 `download_and_decrypt` 的部分逻辑）
   - 验证 adapter 方法被调用

5. `test_adapter_used_in_select_episode`
   - 模拟 `get_adapter().select_episode` 返回 True
   - 调用使用选集的函数（如 `_try_start_episode_on_drama_page`）
   - 验证 adapter 方法被调用

**修改现有测试**：
- 在需要 adapter 的测试中，使用 `set_adapter(mock_adapter)` 初始化
- 确保测试隔离（每个测试后重置 adapter 状态）

**实现要点**：
- 使用 `unittest.mock.patch` 模拟外部依赖
- 使用 `tempfile` 创建临时配置文件
- 测试应该快速运行（< 2 秒）
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py -v -k "config or adapter"</automated>
  </verify>
  <done>
- 测试文件包含至少 5 个 adapter 集成测试用例
- 所有新增测试通过
- 现有测试仍然通过（向后兼容）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → config.yaml | 用户提供的配置文件可能包含恶意内容 |
| Python → app_name 参数 | 配置文件中的 app 字段用于创建 adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | config.yaml | mitigate | 使用 yaml.safe_load（不执行任意代码），验证 app 字段在白名单中 |
| T-04-08 | Denial of Service | load_config | mitigate | 捕获 yaml.YAMLError 和 FileNotFoundError，返回默认配置（向后兼容） |
| T-04-09 | Elevation of Privilege | create_adapter | mitigate | 工厂函数验证 app_name 在注册表中，拒绝未注册的值 |
</threat_model>

<verification>
运行以下命令验证 Plan 03 完成：

```bash
# 1. 验证配置文件存在且可解析
python -c "import yaml; config = yaml.safe_load(open('config.yaml')); print('App:', config['app'])"

# 2. 验证 download_drama.py 可导入
python -c "from scripts.download_drama import load_config, get_adapter, set_adapter; print('Functions imported successfully')"

# 3. 验证 adapter 集成
python -c "from scripts.download_drama import load_config, set_adapter; from scripts.app_adapter import create_adapter; config = load_config(); adapter = create_adapter(config['app']); set_adapter(adapter); print('Adapter integrated successfully')"

# 4. 运行集成测试
pytest tests/test_download_drama.py -v -k "config or adapter"

# 5. 运行所有测试（验证向后兼容）
pytest tests/test_download_drama.py -v
```

所有命令应成功执行且无错误。
</verification>

<success_criteria>
1. config.yaml 配置文件创建完成且格式正确
2. download_drama.py 集成 adapter 架构，所有硬编码逻辑替换为 adapter 调用
3. 向后兼容：配置文件不存在时默认使用 honguo adapter
4. 集成测试覆盖配置加载和 adapter 使用且全部通过
5. 现有测试仍然通过（无破坏性变更）
</success_criteria>

<output>
完成后创建 `.planning/phases/04-app-adapter/04-03-SUMMARY.md`，包含：
- 配置文件格式说明
- adapter 集成的代码变更摘要
- 测试覆盖情况
- 使用示例：如何添加新的 App adapter
- 向后兼容性验证结果
</output>
