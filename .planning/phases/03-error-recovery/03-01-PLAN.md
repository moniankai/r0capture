---
phase: 03-error-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/download_drama.py
  - scripts/drama_download_common.py
autonomous: true
requirements:
  - RECOVERY-01

must_haves:
  truths:
    - 用户下载 80 集短剧时，如果中途脚本崩溃或手动中断，重新运行后能从断点继续
    - 已下载的集数会被自动跳过，不会重复下载
    - session_manifest.jsonl 中记录每次跳过的集数
  artifacts:
    - path: "scripts/download_drama.py"
      provides: "断点续传逻辑：读取 session_manifest.jsonl 识别已下载集数"
      min_lines: 2200
      exports: ["load_completed_episodes", "is_episode_completed"]
    - path: "scripts/drama_download_common.py"
      provides: "会话清单解析工具函数"
      exports: ["parse_session_manifest"]
  key_links:
    - from: "main()"
      to: "load_completed_episodes()"
      via: "启动时加载已完成集数列表"
      pattern: "completed_episodes = load_completed_episodes\\("
    - from: "download_and_decrypt()"
      to: "is_episode_completed()"
      via: "下载前检查集数是否已完成"
      pattern: "if is_episode_completed\\(ep_num"
---

<objective>
实现断点续传机制，使用户在批量下载中断后能从断点继续，避免重复下载已完成的集数。

**Purpose**: 提升长时间批量下载的可靠性，解决用户下载 80 集短剧时中途崩溃需要重新开始的痛点。

**Output**: 
- 读取 session_manifest.jsonl 识别已下载集数的函数
- 下载前检查集数是否已完成的逻辑
- 跳过已完成集数时记录到 session_manifest.jsonl
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-recovery/03-CONTEXT.md
@scripts/download_drama.py
@scripts/drama_download_common.py
</context>

<interfaces>
<!-- 现有 session_manifest.jsonl 格式 -->

从 scripts/download_drama.py 第 2311-2320 行：
```python
append_jsonl(
    session_manifest_path,
    {
        **meta_payload,
        "video_path": dec_path,
        "meta_path": meta_path,
        "status": "downloaded",
        "timestamp": time.time(),
    }
)
```

<!-- 现有文件检查逻辑 -->

从 scripts/download_drama.py 第 2261-2270 行：
```python
if os.path.exists(dec_path) and os.path.getsize(dec_path) > 0:
    logger.info(f"  Episode {actual_episode} already exists, skipping: {dec_path}")
    with open(meta_path, "w", encoding="utf-8") as f:
        json.dump(meta_payload, f, indent=2, ensure_ascii=False)
    append_jsonl(session_manifest_path, {**meta_payload, "video_path": dec_path, "meta_path": meta_path, "status": "skipped_existing"})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现会话清单解析函数</name>
  <files>scripts/drama_download_common.py</files>
  <action>
在 drama_download_common.py 中新增 parse_session_manifest() 函数：
1. 读取 session_manifest.jsonl 文件（每行一个 JSON 对象）
2. 解析每行，提取 episode、video_id、status 字段
3. 返回已完成集数的集合（status 为 "downloaded" 或 "skipped_existing"）
4. 处理文件不存在、格式错误等异常情况（返回空集合）

函数签名：
```python
def parse_session_manifest(manifest_path: str | Path) -> set[int]:
    """解析 session_manifest.jsonl，返回已完成的集数集合。
    
    Args:
        manifest_path: session_manifest.jsonl 文件路径
        
    Returns:
        已完成的集数集合（episode 字段值）
    """
```

注意：
- 使用 Path 对象处理路径
- 使用 json.loads() 逐行解析
- 忽略解析失败的行（记录 warning 日志）
- 只统计 status 为 "downloaded" 或 "skipped_existing" 的记录
  </action>
  <verify>
    <automated>pytest tests/test_drama_download_common.py::test_parse_session_manifest -v</automated>
  </verify>
  <done>parse_session_manifest() 函数存在，能正确解析 session_manifest.jsonl 并返回已完成集数集合</done>
</task>

<task type="auto">
  <name>Task 2: 集成断点续传逻辑到主流程</name>
  <files>scripts/download_drama.py</files>
  <action>
在 download_drama.py 的 main() 函数中集成断点续传：

1. 在 resolve_output_dir() 调用后，立即加载已完成集数：
```python
completed_episodes = set()
if os.path.exists(session_manifest_path):
    from scripts.drama_download_common import parse_session_manifest
    completed_episodes = parse_session_manifest(session_manifest_path)
    if completed_episodes:
        logger.info(f"[断点续传] 检测到 {len(completed_episodes)} 个已完成集数: {sorted(completed_episodes)}")
```

2. 在 download_and_decrypt() 函数开头增加检查：
```python
def download_and_decrypt(ep_num: int) -> dict:
    """在 UI 校验后下载并解密当前捕获的视频。"""
    # 断点续传：检查集数是否已完成
    if ep_num in completed_episodes:
        logger.info(f"[断点续传] 第 {ep_num} 集已完成，跳过")
        # 记录跳过事件到 session_manifest.jsonl
        append_jsonl(session_manifest_path, {
            "episode": ep_num,
            "status": "skipped_resume",
            "timestamp": time.time(),
            "reason": "already_completed"
        })
        return {"success": True, "reason": "skipped_resume", "episode": ep_num}
    
    # 原有逻辑继续...
```

3. 在批量下载循环中处理 skipped_resume 返回值：
- 如果返回 {"success": True, "reason": "skipped_resume"}，视为成功，继续下一集
- 不触发 state.clear() 或重试逻辑

注意：
- completed_episodes 使用 nonlocal 声明（在 main() 内部定义）
- 保持现有的文件存在性检查逻辑（第 2261-2270 行）作为双重保险
- 断点续传检查在 UI 校验之前执行，避免不必要的 UI 操作
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py::test_resume_from_checkpoint -v</automated>
  </verify>
  <done>main() 函数启动时加载已完成集数，download_and_decrypt() 在下载前检查并跳过已完成集数，跳过事件记录到 session_manifest.jsonl</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 测试断点续传功能</name>
  <files>tests/test_drama_download_common.py, tests/test_download_drama.py</files>
  <behavior>
    - 测试 parse_session_manifest() 能正确解析包含多条记录的 jsonl 文件
    - 测试 parse_session_manifest() 能处理文件不存在的情况（返回空集合）
    - 测试 parse_session_manifest() 能忽略格式错误的行
    - 测试 parse_session_manifest() 只统计 status 为 "downloaded" 或 "skipped_existing" 的记录
    - 测试 download_and_decrypt() 在集数已完成时跳过下载并返回 skipped_resume
    - 测试批量下载循环能正确处理 skipped_resume 返回值
  </behavior>
  <action>
在 tests/test_drama_download_common.py 中新增测试：

1. test_parse_session_manifest_success — 正常解析包含 3 条记录的 jsonl 文件
2. test_parse_session_manifest_file_not_exists — 文件不存在时返回空集合
3. test_parse_session_manifest_malformed_lines — 忽略格式错误的行
4. test_parse_session_manifest_filter_by_status — 只统计成功状态的记录

在 tests/test_download_drama.py 中新增测试：

5. test_resume_from_checkpoint — 模拟 session_manifest.jsonl 包含已完成集数，验证 download_and_decrypt() 跳过逻辑
6. test_resume_append_to_manifest — 验证跳过事件正确追加到 session_manifest.jsonl

使用 pytest 的 tmp_path fixture 创建临时文件，使用 unittest.mock.patch 模拟外部依赖。
  </action>
  <verify>
    <automated>pytest tests/test_drama_download_common.py tests/test_download_drama.py -v -k "resume or parse_session_manifest"</automated>
  </verify>
  <done>所有断点续传相关测试通过，覆盖正常场景和边界情况</done>
</task>

</tasks>

<verification>
1. 运行完整测试套件：`pytest tests/ -v`
2. 手动测试：下载 5 集短剧，中途 Ctrl+C 中断，重新运行脚本，验证从断点继续
3. 检查 session_manifest.jsonl 包含 skipped_resume 记录
</verification>

<success_criteria>
- [ ] parse_session_manifest() 函数能正确解析 session_manifest.jsonl
- [ ] main() 启动时加载已完成集数并记录日志
- [ ] download_and_decrypt() 在集数已完成时跳过下载
- [ ] 跳过事件记录到 session_manifest.jsonl（status: skipped_resume）
- [ ] 所有测试通过
- [ ] 手动测试验证断点续传功能正常工作
</success_criteria>

<output>
完成后创建 `.planning/phases/03-error-recovery/03-01-SUMMARY.md`
</output>
