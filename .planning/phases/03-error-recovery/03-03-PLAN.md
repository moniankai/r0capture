---
phase: 03-error-recovery
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - scripts/download_drama.py
  - README.md
autonomous: true
requirements:
  - RECOVERY-03

must_haves:
  truths:
    - session_manifest.jsonl 记录每次下载的完整上下文（集数、video_id、分辨率、成功状态、重试次数、时间戳）
    - 用户可以通过 audit_drama_downloads.py 离线审计下载历史
    - README.md 说明 session_manifest.jsonl 的格式和用途
  artifacts:
    - path: "scripts/download_drama.py"
      provides: "增强的 session_manifest.jsonl 记录格式"
      min_lines: 2300
    - path: "README.md"
      provides: "断点续传和会话清单的使用说明"
      contains: "session_manifest.jsonl"
  key_links:
    - from: "download_and_decrypt()"
      to: "append_jsonl(session_manifest_path)"
      via: "记录完整的下载上下文"
      pattern: "append_jsonl\\(session_manifest_path"
    - from: "audit_drama_downloads.py"
      to: "session_manifest.jsonl"
      via: "读取会话清单进行离线审计"
      pattern: "parse_session_manifest"
---

<objective>
增强会话持久化机制，确保 session_manifest.jsonl 记录完整的下载上下文，便于离线审计和问题排查。

**Purpose**: 提供完整的下载历史记录，支持离线审计工具分析下载质量、识别问题模式。

**Output**: 
- 增强的 session_manifest.jsonl 记录格式（包含所有关键字段）
- README.md 中的使用说明
- 与 audit_drama_downloads.py 的集成验证
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-recovery/03-CONTEXT.md
@.planning/phases/03-error-recovery/03-01-SUMMARY.md
@.planning/phases/03-error-recovery/03-02-SUMMARY.md
@scripts/download_drama.py
@scripts/audit_drama_downloads.py
@README.md
</context>

<interfaces>
<!-- 现有 append_jsonl() 调用位置 -->

从 scripts/download_drama.py：
```python
# 成功下载
append_jsonl(session_manifest_path, {
    **meta_payload,
    "video_path": dec_path,
    "meta_path": meta_path,
    "status": "downloaded",
    "timestamp": time.time(),
})

# 跳过已存在
append_jsonl(session_manifest_path, {
    **meta_payload,
    "video_path": dec_path,
    "meta_path": meta_path,
    "status": "skipped_existing"
})
```

<!-- audit_drama_downloads.py 现有功能 -->

从 scripts/audit_drama_downloads.py：
- 扫描视频目录，识别缺失集数
- 检测重复文件
- 生成重命名建议
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 标准化 session_manifest.jsonl 记录格式</name>
  <files>scripts/download_drama.py</files>
  <action>
统一所有 append_jsonl() 调用，确保记录包含完整的上下文字段：

**标准字段（所有记录必须包含）**：
- episode: int — 集数
- status: str — 状态（downloaded | skipped_existing | skipped_resume | retry_attempt | retry_success | failed_after_retries）
- timestamp: float — Unix 时间戳

**可选字段（根据状态包含）**：
- video_id: str — 视频 ID（8 位后缀）
- resolution: str — 分辨率（720p | 1080p 等）
- video_path: str — 解密后视频路径
- meta_path: str — 元数据文件路径
- retry_count: int — 重试次数（0 表示首次成功）
- reason: str — 失败或跳过原因
- error: str | None — 错误信息

在 download_and_decrypt() 中，将所有 append_jsonl() 调用统一为：

```python
def _log_to_manifest(status: str, **extra_fields) -> None:
    """统一记录到 session_manifest.jsonl"""
    record = {
        "episode": actual_episode or ep_num,
        "status": status,
        "timestamp": time.time(),
        **extra_fields
    }
    append_jsonl(session_manifest_path, record)

# 使用示例
_log_to_manifest("downloaded", video_id=vid, resolution=quality, video_path=dec_path, meta_path=meta_path, retry_count=0)
_log_to_manifest("skipped_existing", video_id=vid, video_path=dec_path)
```

同时在 download_with_retry() 中也使用统一格式。

注意：
- 保持向后兼容（现有字段不删除）
- 使用 **extra_fields 支持灵活扩展
- 所有路径使用相对路径（相对于 output_dir）
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py::test_session_manifest_format -v</automated>
  </verify>
  <done>所有 append_jsonl() 调用使用统一格式，记录包含完整的上下文字段</done>
</task>

<task type="auto">
  <name>Task 2: 更新 README.md 说明断点续传和会话清单</name>
  <files>README.md</files>
  <action>
在 README.md 的"短剧下载器"章节后新增"断点续传与会话清单"小节：

```markdown
### 断点续传与会话清单

#### 自动断点续传

下载器支持自动断点续传。如果批量下载中途中断（脚本崩溃、手动 Ctrl+C、网络断开等），重新运行相同命令即可从断点继续：

```bash
# 首次运行：下载到第 20 集时中断
python scripts/download_drama.py -n "剧名" --search -b 80

# 重新运行：自动跳过前 20 集，从第 21 集继续
python scripts/download_drama.py -n "剧名" --search -b 80
```

断点续传基于 `session_manifest.jsonl` 文件，该文件记录每次下载的历史。

#### 自动重试机制

当单集下载失败时（网络超时、解密失败等），脚本自动重试最多 3 次：
- 每次重试前清空 Hook 数据状态，确保数据刷新
- 重试间隔 2 秒，避免触发 App 限流
- 重试历史记录到 `session_manifest.jsonl`

#### session_manifest.jsonl 格式

每次下载会在输出目录生成 `session_manifest.jsonl` 文件（每行一个 JSON 对象）：

```jsonl
{"episode": 1, "status": "downloaded", "video_id": "abc12345", "resolution": "720p", "video_path": "episode_001_abc12345.mp4", "retry_count": 0, "timestamp": 1713196800.0}
{"episode": 2, "status": "retry_attempt", "attempt": 1, "reason": "download_failed", "timestamp": 1713196850.0}
{"episode": 2, "status": "retry_success", "video_id": "def67890", "resolution": "720p", "video_path": "episode_002_def67890.mp4", "retry_count": 1, "timestamp": 1713196860.0}
{"episode": 3, "status": "skipped_resume", "reason": "already_completed", "timestamp": 1713196900.0}
```

**字段说明**：
- `episode`: 集数
- `status`: 状态（downloaded | skipped_existing | skipped_resume | retry_attempt | retry_success | failed_after_retries）
- `video_id`: 视频 ID（8 位后缀）
- `resolution`: 分辨率
- `video_path`: 解密后视频路径（相对路径）
- `retry_count`: 重试次数（0 表示首次成功）
- `timestamp`: Unix 时间戳

#### 离线审计

使用 `audit_drama_downloads.py` 审计下载质量：

```bash
python scripts/audit_drama_downloads.py videos/剧名 --expected-total 80
```

审计工具会：
- 识别缺失的集数
- 检测重复文件
- 分析 `session_manifest.jsonl` 中的重试模式
- 生成重命名建议
```

注意：
- 使用清晰的示例和代码块
- 说明断点续传的工作原理（基于 session_manifest.jsonl）
- 提供 session_manifest.jsonl 的实际示例
  </action>
  <verify>
    <automated>grep -q "断点续传" README.md && grep -q "session_manifest.jsonl" README.md</automated>
  </verify>
  <done>README.md 包含断点续传和会话清单的完整说明，包括格式示例和使用方法</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 验证会话清单与审计工具的集成</name>
  <files>tests/test_download_drama.py, tests/test_audit_drama_downloads.py</files>
  <behavior>
    - 测试 session_manifest.jsonl 记录包含所有必需字段
    - 测试 parse_session_manifest() 能正确解析增强后的记录格式
    - 测试 audit_drama_downloads.py 能读取 session_manifest.jsonl 并分析重试模式
    - 测试不同状态（downloaded、skipped_resume、retry_success）的记录格式一致性
  </behavior>
  <action>
在 tests/test_download_drama.py 中新增测试：

1. test_session_manifest_format_downloaded — 验证 downloaded 状态记录包含所有必需字段
2. test_session_manifest_format_skipped_resume — 验证 skipped_resume 状态记录格式
3. test_session_manifest_format_retry_success — 验证 retry_success 状态记录包含 retry_count
4. test_session_manifest_backward_compatible — 验证 parse_session_manifest() 能解析旧格式记录

在 tests/test_audit_drama_downloads.py 中新增测试（如果该文件不存在则创建）：

5. test_audit_reads_session_manifest — 验证审计工具能读取 session_manifest.jsonl
6. test_audit_identifies_retry_patterns — 验证审计工具能识别重试模式（如某集重试 3 次）

使用 pytest 的 tmp_path fixture 创建临时文件，模拟不同的 session_manifest.jsonl 内容。
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py tests/test_audit_drama_downloads.py -v -k "manifest or audit"</automated>
  </verify>
  <done>所有会话清单相关测试通过，验证记录格式一致性和审计工具集成</done>
</task>

</tasks>

<verification>
1. 运行完整测试套件：`pytest tests/ -v`
2. 手动测试：下载 5 集短剧，检查 session_manifest.jsonl 格式
3. 运行 audit_drama_downloads.py 验证能正确读取会话清单
4. 检查 README.md 说明是否清晰易懂
</verification>

<success_criteria>
- [ ] 所有 append_jsonl() 调用使用统一格式
- [ ] session_manifest.jsonl 记录包含所有必需字段（episode、status、timestamp）
- [ ] README.md 包含断点续传和会话清单的完整说明
- [ ] parse_session_manifest() 能正确解析增强后的记录格式
- [ ] 所有测试通过
- [ ] 手动测试验证会话清单格式正确
</success_criteria>

<output>
完成后创建 `.planning/phases/03-error-recovery/03-03-SUMMARY.md`
</output>
