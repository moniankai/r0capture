---
phase: 03-error-recovery
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/download_drama.py
autonomous: true
requirements:
  - RECOVERY-02

must_haves:
  truths:
    - 当单集下载失败时，脚本自动重试最多 3 次
    - 每次重试前清空 CaptureState，确保 Hook 数据刷新
    - session_manifest.jsonl 中记录每次重试的历史（包括失败原因和重试次数）
  artifacts:
    - path: "scripts/download_drama.py"
      provides: "自动重试逻辑：max_retries 配置和重试循环"
      min_lines: 2250
      exports: ["download_with_retry"]
  key_links:
    - from: "批量下载循环"
      to: "download_with_retry()"
      via: "包装 download_and_decrypt() 增加重试逻辑"
      pattern: "result = download_with_retry\\(ep_num"
    - from: "download_with_retry()"
      to: "reset_capture_state()"
      via: "每次重试前清空状态"
      pattern: "reset_capture_state\\(\\)"
---

<objective>
实现自动重试机制，当单集下载失败时自动重试最多 3 次，每次重试前清空 Hook 数据状态。

**Purpose**: 提升批量下载的健壮性，自动处理网络超时、解密失败等临时性错误，减少用户手动干预。

**Output**: 
- download_with_retry() 包装函数，封装重试逻辑
- 每次重试前调用 reset_capture_state() 清空状态
- 重试历史记录到 session_manifest.jsonl
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-recovery/03-CONTEXT.md
@.planning/phases/03-error-recovery/03-01-SUMMARY.md
@scripts/download_drama.py
</context>

<interfaces>
<!-- 现有 reset_capture_state() 函数 -->

从 Phase 01-01 实现的模块级函数：
```python
def reset_capture_state() -> None:
    """重置全局 CaptureState 实例（用于测试或重试场景）"""
    global _capture_state_instance
    _capture_state_instance = CaptureState()
```

<!-- 现有 download_and_decrypt() 返回格式 -->

从 scripts/download_drama.py：
```python
# 成功返回
{"success": True, "episode": ep_num, "video_path": dec_path}

# 失败返回
{"success": False, "reason": "missing_episode"}
{"success": False, "reason": "title_mismatch"}
{"success": False, "reason": "download_failed", "error": str(e)}
```

<!-- 现有 session_manifest.jsonl 记录格式 -->

```python
append_jsonl(session_manifest_path, {
    "episode": ep_num,
    "video_id": vid,
    "status": "downloaded",
    "timestamp": time.time()
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现 download_with_retry() 包装函数</name>
  <files>scripts/download_drama.py</files>
  <action>
在 main() 函数内部（download_and_decrypt() 定义之后）新增 download_with_retry() 函数：

```python
def download_with_retry(ep_num: int, max_retries: int = 3) -> dict:
    """包装 download_and_decrypt()，增加自动重试逻辑。
    
    Args:
        ep_num: 集数
        max_retries: 最大重试次数（默认 3 次）
        
    Returns:
        download_and_decrypt() 的返回值（最后一次尝试的结果）
    """
    for attempt in range(max_retries):
        if attempt > 0:
            logger.warning(f"[重试] 第 {ep_num} 集下载失败，开始第 {attempt + 1}/{max_retries} 次重试")
            # 重试前清空 Hook 数据状态
            reset_capture_state()
            logger.debug(f"[重试] 已清空 CaptureState，等待 2 秒后重试")
            time.sleep(2)
        
        result = download_and_decrypt(ep_num)
        
        # 记录重试历史到 session_manifest.jsonl
        if attempt > 0 or not result.get("success", False):
            append_jsonl(session_manifest_path, {
                "episode": ep_num,
                "status": "retry_attempt" if not result.get("success", False) else "retry_success",
                "attempt": attempt + 1,
                "max_retries": max_retries,
                "reason": result.get("reason", "unknown"),
                "error": result.get("error", None),
                "timestamp": time.time()
            })
        
        # 成功或跳过（skipped_resume）则返回
        if result.get("success", False):
            return result
        
        # 最后一次尝试失败，记录最终失败状态
        if attempt == max_retries - 1:
            logger.error(f"[重试] 第 {ep_num} 集重试 {max_retries} 次后仍失败: {result.get('reason', 'unknown')}")
            append_jsonl(session_manifest_path, {
                "episode": ep_num,
                "status": "failed_after_retries",
                "max_retries": max_retries,
                "final_reason": result.get("reason", "unknown"),
                "final_error": result.get("error", None),
                "timestamp": time.time()
            })
    
    return result
```

注意：
- 使用 nonlocal 声明访问 session_manifest_path
- 第一次尝试（attempt=0）不清空状态，直接使用当前 Hook 数据
- 重试间隔 2 秒，避免过快重试触发 App 限流
- skipped_resume 视为成功，不触发重试
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py::test_download_with_retry -v</automated>
  </verify>
  <done>download_with_retry() 函数存在，能正确处理重试逻辑并记录到 session_manifest.jsonl</done>
</task>

<task type="auto">
  <name>Task 2: 集成重试逻辑到批量下载循环</name>
  <files>scripts/download_drama.py</files>
  <action>
在 main() 函数的批量下载循环中，将 download_and_decrypt() 调用替换为 download_with_retry()：

定位批量下载循环（约第 2400-2500 行），找到类似以下代码：
```python
for i in range(batch_count):
    # ... 上滑逻辑 ...
    result = download_and_decrypt(target_episode)
    if not result.get("success", False):
        # 错误处理
```

替换为：
```python
for i in range(batch_count):
    # ... 上滑逻辑 ...
    result = download_with_retry(target_episode, max_retries=3)
    if not result.get("success", False):
        logger.error(f"第 {target_episode} 集下载失败（已重试 3 次）: {result.get('reason', 'unknown')}")
        # 继续下一集或中断（根据现有逻辑）
```

同时在单集下载模式（batch_count=1）中也使用 download_with_retry()。

注意：
- 保持现有的错误处理逻辑（是否继续下一集或中断）
- 不修改 download_and_decrypt() 内部逻辑
- max_retries=3 硬编码（未来可改为命令行参数）
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py::test_batch_download_with_retry -v</automated>
  </verify>
  <done>批量下载循环使用 download_with_retry() 替代 download_and_decrypt()，失败时自动重试最多 3 次</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 测试自动重试功能</name>
  <files>tests/test_download_drama.py</files>
  <behavior>
    - 测试 download_with_retry() 在第一次成功时不触发重试
    - 测试 download_with_retry() 在第一次失败、第二次成功时重试 1 次
    - 测试 download_with_retry() 在连续失败 3 次后返回失败结果
    - 测试每次重试前调用 reset_capture_state() 清空状态
    - 测试重试历史正确记录到 session_manifest.jsonl（包括 attempt、reason、error）
    - 测试 skipped_resume 不触发重试
  </behavior>
  <action>
在 tests/test_download_drama.py 中新增测试：

1. test_download_with_retry_success_first_attempt — 第一次成功，不重试
2. test_download_with_retry_success_after_one_retry — 第一次失败，第二次成功
3. test_download_with_retry_fail_after_max_retries — 连续失败 3 次
4. test_download_with_retry_clears_state — 验证每次重试前调用 reset_capture_state()
5. test_download_with_retry_logs_to_manifest — 验证重试历史记录到 session_manifest.jsonl
6. test_download_with_retry_skips_resume — skipped_resume 不触发重试

使用 unittest.mock.patch 模拟：
- download_and_decrypt() 返回不同结果
- reset_capture_state() 调用
- append_jsonl() 调用
- time.sleep() 避免测试等待

验证：
- reset_capture_state() 调用次数 = 重试次数
- append_jsonl() 记录包含正确的 attempt、status、reason 字段
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py -v -k "retry"</automated>
  </verify>
  <done>所有自动重试相关测试通过，覆盖成功、失败、重试次数等场景</done>
</task>

</tasks>

<verification>
1. 运行完整测试套件：`pytest tests/ -v`
2. 手动测试：模拟网络不稳定场景（临时断网），验证自动重试
3. 检查 session_manifest.jsonl 包含 retry_attempt、retry_success、failed_after_retries 记录
4. 验证重试间隔约 2 秒（观察日志时间戳）
</verification>

<success_criteria>
- [ ] download_with_retry() 函数能正确处理重试逻辑
- [ ] 每次重试前调用 reset_capture_state() 清空状态
- [ ] 重试历史记录到 session_manifest.jsonl（包含 attempt、reason、error）
- [ ] 批量下载循环使用 download_with_retry() 替代 download_and_decrypt()
- [ ] 所有测试通过
- [ ] 手动测试验证自动重试功能正常工作
</success_criteria>

<output>
完成后创建 `.planning/phases/03-error-recovery/03-02-SUMMARY.md`
</output>
