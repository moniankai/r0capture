---
phase: 01-core-stability
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - tests/test_capture_state.py
  - tests/test_ui_stability.py
  - tests/test_download_drama.py
autonomous: true
requirements:
  - STABILITY-01
  - STABILITY-02
  - STABILITY-03

must_haves:
  truths:
    - 单元测试覆盖单例模式、时间戳过滤、UI 稳定性检查
    - 集成测试验证批量下载无内容错位
    - 现有测试套件全部通过（无回归）
  artifacts:
    - path: "tests/test_capture_state.py"
      provides: "单例模式和时间戳过滤测试"
      min_lines: 100
    - path: "tests/test_ui_stability.py"
      provides: "UI 稳定性检查测试"
      min_lines: 150
    - path: "tests/test_download_drama.py"
      provides: "回归测试（现有测试保持通过）"
      min_lines: 500
  key_links:
    - from: "test_capture_state.py"
      to: "get_capture_state()"
      via: "测试单例模式"
      pattern: "test_singleton_pattern"
    - from: "test_ui_stability.py"
      to: "wait_for_ui_stable()"
      via: "测试 UI 轮询逻辑"
      pattern: "test_wait_for_ui_stable"
---

<objective>
为 Phase 1 的所有修改创建全面的测试覆盖，确保单例模式、时间戳过滤、UI 稳定性检查正确工作，并验证无回归。

**Purpose**: 建立测试基线，确保重构后的代码质量和可维护性，防止未来修改引入 bug。

**Output**: 
- `tests/test_capture_state.py` — 单例模式和时间戳过滤测试
- `tests/test_ui_stability.py` — UI 稳定性检查测试
- 更新 `tests/test_download_drama.py` — 确保现有测试通过
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-stability/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@scripts/download_drama.py
@scripts/drama_download_common.py
@tests/test_download_drama.py

# 依赖前序计划的成果
# 01-01-PLAN: get_capture_state() 和 reset_capture_state()
# 01-02-PLAN: VideoRef 带 timestamp 字段，时间戳过滤逻辑
# 01-03-PLAN: wait_for_ui_stable() 函数
</context>

<interfaces>
<!-- 需要测试的函数签名 -->

从 scripts/download_drama.py（01-01-PLAN 产出）：
```python
def get_capture_state() -> CaptureState:
    """获取全局 CaptureState 单例"""

def reset_capture_state():
    """重置全局 CaptureState"""
```

从 scripts/download_drama.py（01-02-PLAN 产出）：
```python
@dataclass
class VideoRef:
    video_id: str
    url: str
    resolution: str
    timestamp: float = 0.0
    context: Dict[str, Any] = field(default_factory=dict)
```

从 scripts/download_drama.py（01-03-PLAN 产出）：
```python
def wait_for_ui_stable(
    expected_ep: int,
    timeout: float = 10.0,
    poll_interval: float = 0.5
) -> bool:
    """等待 UI 稳定（集数匹配预期）"""
```

<!-- 现有测试文件结构 -->

从 tests/test_download_drama.py：
```python
# 现有测试包含 UI 解析、会话校验、文件名生成等
# 需要确保这些测试在重构后仍然通过
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建单例模式和时间戳过滤测试</name>
  <files>tests/test_capture_state.py</files>
  <action>
创建新文件 `tests/test_capture_state.py`，测试 01-01-PLAN 和 01-02-PLAN 的功能：

```python
"""测试 CaptureState 单例模式和时间戳过滤逻辑"""

import sys
import time
from pathlib import Path

# 添加 scripts 目录到路径
sys.path.insert(0, str(Path(__file__).parent.parent / "scripts"))

import pytest
from download_drama import (
    get_capture_state,
    reset_capture_state,
    CaptureState,
    VideoRef,
)


class TestCaptureStateSingleton:
    """测试单例模式"""

    def test_singleton_pattern(self):
        """测试 get_capture_state 返回同一实例"""
        state1 = get_capture_state()
        state2 = get_capture_state()
        assert state1 is state2, "get_capture_state 应返回同一实例"

    def test_reset_creates_new_instance(self):
        """测试 reset_capture_state 创建新实例"""
        state1 = get_capture_state()
        state1.video_refs.append(
            VideoRef("test_id", "http://test", "1080p", timestamp=time.time())
        )
        
        reset_capture_state()
        state2 = get_capture_state()
        
        assert state1 is not state2, "reset 后应返回新实例"
        assert len(state2.video_refs) == 0, "新实例应为空"

    def test_state_isolation_after_reset(self):
        """测试 reset 后状态隔离"""
        state1 = get_capture_state()
        state1.video_urls.append("http://old_url")
        state1.aes_keys.append("old_key")
        
        reset_capture_state()
        state2 = get_capture_state()
        
        assert len(state2.video_urls) == 0, "新实例不应包含旧 URL"
        assert len(state2.aes_keys) == 0, "新实例不应包含旧密钥"


class TestVideoRefTimestamp:
    """测试 VideoRef 时间戳字段"""

    def test_videoref_has_timestamp_field(self):
        """测试 VideoRef 包含 timestamp 字段"""
        ref = VideoRef("id1", "http://test", "1080p", timestamp=123.456)
        assert hasattr(ref, "timestamp"), "VideoRef 应有 timestamp 字段"
        assert ref.timestamp == 123.456, "timestamp 值应正确"

    def test_videoref_default_timestamp(self):
        """测试 VideoRef 默认 timestamp 为 0.0"""
        ref = VideoRef("id1", "http://test", "1080p")
        assert ref.timestamp == 0.0, "默认 timestamp 应为 0.0"

    def test_videoref_has_context_field(self):
        """测试 VideoRef 包含 context 字段"""
        ref = VideoRef("id1", "http://test", "1080p")
        assert hasattr(ref, "context"), "VideoRef 应有 context 字段"
        assert isinstance(ref.context, dict), "context 应为字典"
        assert len(ref.context) == 0, "默认 context 应为空"


class TestTimestampFiltering:
    """测试时间戳过滤逻辑"""

    def test_filter_recent_data(self):
        """测试过滤最近 5 秒内的数据"""
        now = time.time()
        
        refs = [
            VideoRef("id1", "http://test1", "1080p", timestamp=now - 10),  # 过期
            VideoRef("id2", "http://test2", "720p", timestamp=now - 2),    # 新鲜
            VideoRef("id3", "http://test3", "1080p", timestamp=now - 0.5), # 新鲜
        ]
        
        FRESHNESS_THRESHOLD = 5.0
        recent = [r for r in refs if now - r.timestamp < FRESHNESS_THRESHOLD]
        
        assert len(recent) == 2, "应过滤出 2 个新鲜数据"
        assert recent[0].video_id == "id2"
        assert recent[1].video_id == "id3"

    def test_select_newest_data(self):
        """测试选择最新的数据"""
        now = time.time()
        
        refs = [
            VideoRef("id1", "http://test1", "1080p", timestamp=now - 3),
            VideoRef("id2", "http://test2", "720p", timestamp=now - 1),  # 最新
            VideoRef("id3", "http://test3", "1080p", timestamp=now - 2),
        ]
        
        newest = max(refs, key=lambda r: r.timestamp)
        assert newest.video_id == "id2", "应选择最新的数据"

    def test_all_data_expired(self):
        """测试所有数据都过期的情况"""
        now = time.time()
        
        refs = [
            VideoRef("id1", "http://test1", "1080p", timestamp=now - 10),
            VideoRef("id2", "http://test2", "720p", timestamp=now - 20),
        ]
        
        FRESHNESS_THRESHOLD = 5.0
        recent = [r for r in refs if now - r.timestamp < FRESHNESS_THRESHOLD]
        
        assert len(recent) == 0, "所有数据都应被过滤"


if __name__ == "__main__":
    pytest.main([__file__, "-v"])
```

**关键点**：
- 使用 pytest 框架
- 测试类按功能分组（单例、时间戳、过滤）
- 每个测试函数有清晰的 docstring
- 测试覆盖正常场景和边界条件
- 可独立运行（`python tests/test_capture_state.py`）
  </action>
  <verify>
    <automated>pytest tests/test_capture_state.py -v</automated>
  </verify>
  <done>
- `tests/test_capture_state.py` 文件存在
- 包含至少 8 个测试函数
- 所有测试通过（pytest 返回 0）
- 测试覆盖单例模式、时间戳字段、过滤逻辑
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 UI 稳定性检查测试</name>
  <files>tests/test_ui_stability.py</files>
  <action>
创建新文件 `tests/test_ui_stability.py`，测试 01-03-PLAN 的 `wait_for_ui_stable()` 函数：

```python
"""测试 UI 稳定性检查逻辑"""

import sys
import time
from pathlib import Path
from unittest.mock import patch, MagicMock

# 添加 scripts 目录到路径
sys.path.insert(0, str(Path(__file__).parent.parent / "scripts"))

import pytest
from drama_download_common import UIContext


class TestWaitForUIStable:
    """测试 wait_for_ui_stable 函数"""

    @patch("download_drama.detect_ui_context_from_device")
    def test_ui_stable_immediately(self, mock_detect):
        """测试 UI 立即稳定的情况"""
        from download_drama import wait_for_ui_stable
        
        # 模拟 UI 立即返回正确集数
        mock_detect.return_value = UIContext(
            title="测试剧",
            current_episode=5,
            total_episodes=10
        )
        
        result = wait_for_ui_stable(expected_ep=5, timeout=10.0)
        
        assert result is True, "UI 匹配时应返回 True"
        assert mock_detect.call_count >= 1, "应至少调用一次 detect"

    @patch("download_drama.detect_ui_context_from_device")
    def test_ui_stable_after_delay(self, mock_detect):
        """测试 UI 延迟后稳定的情况"""
        from download_drama import wait_for_ui_stable
        
        # 模拟 UI 先返回旧集数，后返回新集数
        mock_detect.side_effect = [
            UIContext("测试剧", 3, 10),  # 第 1 次：旧集数
            UIContext("测试剧", 3, 10),  # 第 2 次：仍是旧集数
            UIContext("测试剧", 5, 10),  # 第 3 次：新集数
        ]
        
        result = wait_for_ui_stable(expected_ep=5, timeout=10.0, poll_interval=0.1)
        
        assert result is True, "UI 最终匹配时应返回 True"
        assert mock_detect.call_count == 3, "应调用 3 次 detect"

    @patch("download_drama.detect_ui_context_from_device")
    def test_ui_timeout(self, mock_detect):
        """测试 UI 超时的情况"""
        from download_drama import wait_for_ui_stable
        
        # 模拟 UI 始终返回错误集数
        mock_detect.return_value = UIContext("测试剧", 3, 10)
        
        result = wait_for_ui_stable(expected_ep=5, timeout=1.0, poll_interval=0.2)
        
        assert result is False, "超时时应返回 False"
        assert mock_detect.call_count >= 4, "应多次尝试"

    @patch("download_drama.detect_ui_context_from_device")
    def test_ui_parse_failure(self, mock_detect):
        """测试 UI 解析失败的情况"""
        from download_drama import wait_for_ui_stable
        
        # 模拟 UI 解析失败（返回 None）
        mock_detect.side_effect = [
            None,  # 第 1 次：解析失败
            None,  # 第 2 次：解析失败
            UIContext("测试剧", 5, 10),  # 第 3 次：成功
        ]
        
        result = wait_for_ui_stable(expected_ep=5, timeout=10.0, poll_interval=0.1)
        
        assert result is True, "最终成功时应返回 True"
        assert mock_detect.call_count == 3, "应调用 3 次 detect"

    @patch("download_drama.detect_ui_context_from_device")
    def test_ui_always_fails(self, mock_detect):
        """测试 UI 始终解析失败的情况"""
        from download_drama import wait_for_ui_stable
        
        # 模拟 UI 始终解析失败
        mock_detect.return_value = None
        
        result = wait_for_ui_stable(expected_ep=5, timeout=1.0, poll_interval=0.2)
        
        assert result is False, "始终失败时应返回 False"
        assert mock_detect.call_count >= 4, "应多次尝试"

    @patch("download_drama.detect_ui_context_from_device")
    def test_poll_interval_respected(self, mock_detect):
        """测试轮询间隔是否生效"""
        from download_drama import wait_for_ui_stable
        
        # 模拟 UI 始终返回错误集数
        mock_detect.return_value = UIContext("测试剧", 3, 10)
        
        start = time.time()
        result = wait_for_ui_stable(expected_ep=5, timeout=1.0, poll_interval=0.3)
        elapsed = time.time() - start
        
        assert result is False, "超时时应返回 False"
        assert elapsed >= 1.0, "应至少等待 timeout 时间"
        assert elapsed < 1.5, "不应超时太多"


class TestTwoPhaseDownload:
    """测试两阶段下载模式集成"""

    @patch("download_drama.select_episode_from_ui")
    @patch("download_drama.wait_for_ui_stable")
    @patch("download_drama.get_capture_state")
    def test_ui_stable_success(self, mock_state, mock_wait, mock_select):
        """测试 UI 稳定成功的情况"""
        # 此测试需要 download_and_decrypt 函数的完整实现
        # 暂时跳过，留待集成测试
        pass

    @patch("download_drama.select_episode_from_ui")
    @patch("download_drama.wait_for_ui_stable")
    @patch("download_drama.reset_capture_state")
    def test_ui_stable_failure_clears_state(self, mock_reset, mock_wait, mock_select):
        """测试 UI 未稳定时清空 state"""
        # 此测试需要 download_and_decrypt 函数的完整实现
        # 暂时跳过，留待集成测试
        pass


if __name__ == "__main__":
    pytest.main([__file__, "-v"])
```

**关键点**：
- 使用 `unittest.mock.patch` 模拟 `detect_ui_context_from_device`
- 测试正常场景（立即稳定、延迟稳定）
- 测试异常场景（超时、解析失败）
- 测试轮询间隔是否生效
- 集成测试部分留待后续（需要完整的 `download_and_decrypt` 实现）
  </action>
  <verify>
    <automated>pytest tests/test_ui_stability.py -v</automated>
  </verify>
  <done>
- `tests/test_ui_stability.py` 文件存在
- 包含至少 6 个测试函数
- 所有测试通过（pytest 返回 0）
- 测试覆盖 UI 稳定性检查的各种场景
  </done>
</task>

<task type="auto">
  <name>Task 3: 运行回归测试并修复兼容性问题</name>
  <files>tests/test_download_drama.py</files>
  <action>
运行现有测试套件，确保 Phase 1 的重构没有破坏现有功能：

**步骤 1: 运行现有测试**

```bash
pytest tests/test_download_drama.py -v
```

**步骤 2: 如果测试失败，分析原因并修复**

可能的失败原因：
1. **导入错误** — 如果测试文件导入了被移除的函数（如 `clear_state_fn`），更新导入语句
2. **函数签名变化** — 如果测试调用了修改后的函数（如 `_try_start_episode_on_drama_page`），更新调用方式
3. **Mock 失效** — 如果测试 mock 了被重构的函数，更新 mock 目标

**修复示例**

如果测试中有类似代码：
```python
# 旧代码
def test_try_start_episode():
    clear_fn = MagicMock()
    result = _try_start_episode_on_drama_page(5, clear_state_fn=clear_fn)
    assert clear_fn.called
```

修改为：
```python
# 新代码
@patch("download_drama.reset_capture_state")
def test_try_start_episode(mock_reset):
    result = _try_start_episode_on_drama_page(5)
    # 如果函数内部调用了 reset，验证调用
    # assert mock_reset.called
```

**步骤 3: 确保所有测试通过**

```bash
pytest tests/ -v
```

**关键点**：
- 不修改测试的业务逻辑，仅修复兼容性问题
- 如果测试依赖旧的 API，更新为新的 API
- 确保测试覆盖率不降低
- 所有测试必须通过（绿色）
  </action>
  <verify>
    <automated>pytest tests/test_download_drama.py -v</automated>
  </verify>
  <done>
- 现有测试套件全部通过（pytest 返回 0）
- 无测试被跳过或标记为 xfail
- 测试覆盖率保持或提升
- 修复的兼容性问题已记录在 SUMMARY 中
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建测试运行脚本和文档</name>
  <files>tests/README.md</files>
  <action>
创建 `tests/README.md` 文档，说明如何运行测试：

```markdown
# 测试指南

## 运行所有测试

```bash
pytest tests/ -v
```

## 运行特定测试文件

```bash
# 单例模式和时间戳过滤测试
pytest tests/test_capture_state.py -v

# UI 稳定性检查测试
pytest tests/test_ui_stability.py -v

# 现有功能回归测试
pytest tests/test_download_drama.py -v

# 审计功能测试
pytest tests/test_audit_drama_downloads.py -v
```

## 测试覆盖率

```bash
pytest tests/ --cov=scripts --cov-report=html
# 查看 htmlcov/index.html
```

## 测试结构

- `test_capture_state.py` — 测试 CaptureState 单例模式和时间戳过滤（Phase 1）
- `test_ui_stability.py` — 测试 UI 稳定性检查逻辑（Phase 1）
- `test_download_drama.py` — 测试 UI 解析、会话校验、文件名生成（现有）
- `test_audit_drama_downloads.py` — 测试审计功能（现有）

## Phase 1 测试覆盖

### 单例模式（01-01-PLAN）
- ✓ `get_capture_state()` 返回同一实例
- ✓ `reset_capture_state()` 创建新实例
- ✓ 状态隔离

### 时间戳过滤（01-02-PLAN）
- ✓ VideoRef 包含 timestamp 字段
- ✓ 过滤最近 5 秒内的数据
- ✓ 选择最新的数据
- ✓ 所有数据过期的情况

### UI 稳定性检查（01-03-PLAN）
- ✓ UI 立即稳定
- ✓ UI 延迟后稳定
- ✓ UI 超时
- ✓ UI 解析失败
- ✓ 轮询间隔生效

## 添加新测试

1. 在 `tests/` 目录创建 `test_<module>.py` 文件
2. 使用 pytest 框架编写测试函数
3. 运行 `pytest tests/test_<module>.py -v` 验证
4. 更新本文档

## 持续集成

测试应在每次提交前运行：

```bash
# 快速测试（跳过慢速测试）
pytest tests/ -v -m "not slow"

# 完整测试
pytest tests/ -v
```
```

**关键点**：
- 清晰的运行指令
- 测试文件结构说明
- Phase 1 测试覆盖清单
- 添加新测试的指引
  </action>
  <verify>
    <automated>test -f tests/README.md && echo "✓ tests/README.md exists" || echo "❌ File not found"</automated>
  </verify>
  <done>
- `tests/README.md` 文件存在
- 包含运行测试的清晰指令
- 列出 Phase 1 的测试覆盖
- 提供添加新测试的指引
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试 → 生产代码 | 测试代码通过 mock 隔离外部依赖 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Mock 对象 | accept | 测试环境中 mock 可信，不影响生产环境 |
| T-01-08 | Information Disclosure | 测试数据 | accept | 测试使用假数据，无敏感信息 |
</threat_model>

<verification>
**运行所有测试**：
```bash
pytest tests/ -v
```

**检查测试覆盖率**：
```bash
pytest tests/ --cov=scripts --cov-report=term-missing
```

**验证新测试文件存在**：
```bash
ls -la tests/test_capture_state.py
ls -la tests/test_ui_stability.py
ls -la tests/README.md
```

**验证测试数量**：
```bash
pytest tests/ --collect-only | grep "test session starts"
```
</verification>

<success_criteria>
- [ ] `tests/test_capture_state.py` 存在且包含至少 8 个测试
- [ ] `tests/test_ui_stability.py` 存在且包含至少 6 个测试
- [ ] 所有新测试通过（pytest 返回 0）
- [ ] 现有测试套件全部通过（无回归）
- [ ] `tests/README.md` 文档存在且完整
- [ ] 测试覆盖 Phase 1 的所有核心功能
</success_criteria>

<output>
完成后创建 `.planning/phases/01-core-stability/01-04-SUMMARY.md`，包含：
- 新增的测试文件和测试函数列表
- 测试覆盖率报告
- 回归测试结果
- 修复的兼容性问题（如有）
</output>
