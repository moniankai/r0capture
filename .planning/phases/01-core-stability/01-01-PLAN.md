---
phase: 01-core-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/download_drama.py
autonomous: true
requirements:
  - STABILITY-01
  - STABILITY-02

must_haves:
  truths:
    - 所有模块都能直接访问 CaptureState，无需通过回调传递
    - 测试代码可以注入自定义 CaptureState 实例
    - 现有功能保持向后兼容（无破坏性变更）
  artifacts:
    - path: "scripts/download_drama.py"
      provides: "模块级单例 get_capture_state() 和 reset_capture_state()"
      min_lines: 2173
      exports: ["get_capture_state", "reset_capture_state"]
  key_links:
    - from: "_try_start_episode_on_drama_page"
      to: "get_capture_state()"
      via: "直接调用获取全局状态"
      pattern: "state = get_capture_state\\(\\)"
    - from: "search_drama_in_app"
      to: "reset_capture_state()"
      via: "直接调用清空全局状态"
      pattern: "reset_capture_state\\(\\)"
    - from: "main()"
      to: "get_capture_state()"
      via: "初始化全局状态"
      pattern: "state = get_capture_state\\(\\)"
---

<objective>
将 CaptureState 从 main() 函数内部提升为模块级单例，消除作用域问题和回调传递复杂度。

**Purpose**: 解决 CONCERNS.md 中记录的状态管理混乱问题（第 31-41 行），使所有模块级函数都能直接访问状态，同时保持测试友好性。

**Output**: 
- 模块级单例访问器 `get_capture_state()` 和 `reset_capture_state()`
- 移除所有 `clear_state_fn` 回调参数
- 保持现有功能完全向后兼容
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-stability/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@scripts/download_drama.py
</context>

<interfaces>
<!-- 当前 CaptureState 定义（将保持不变） -->

从 scripts/download_drama.py 第 201-273 行：
```python
class CaptureState:
    """封装单次捕获轮次的所有 Hook 数据"""
    def __init__(self):
        self.video_urls: List[str] = []
        self.aes_keys: List[str] = []
        self.video_refs: List[VideoRef] = []
        self.video_models: List[Dict[str, Any]] = []
    
    def clear(self):
        """清空所有捕获数据"""
        self.video_urls.clear()
        self.aes_keys.clear()
        self.video_refs.clear()
        self.video_models.clear()
    
    def best_video(self) -> Optional[VideoRef]:
        """根据分辨率和编码选择最佳视频"""
        # ... 实现逻辑
```

<!-- 需要修改的函数签名 -->

从 scripts/download_drama.py 第 1206 行：
```python
def _try_start_episode_on_drama_page(
    ep_num: int,
    clear_state_fn: Callable[[], None]  # 将移除此参数
) -> bool:
    """尝试从剧集页面启动指定集数"""
```

从 scripts/download_drama.py 第 1322 行：
```python
def search_drama_in_app(
    drama_name: str,
    clear_state_fn: Callable[[], None]  # 将移除此参数
) -> bool:
    """在 App 内搜索短剧并打开"""
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建模块级单例访问器</name>
  <files>scripts/download_drama.py</files>
  <action>
在 `scripts/download_drama.py` 文件顶部（CaptureState 类定义之后，约第 274 行），添加模块级单例实现：

```python
# 模块级全局状态（单例模式）
_global_capture_state: Optional[CaptureState] = None

def get_capture_state() -> CaptureState:
    """获取全局 CaptureState 单例
    
    Returns:
        CaptureState: 全局捕获状态实例
    """
    global _global_capture_state
    if _global_capture_state is None:
        _global_capture_state = CaptureState()
    return _global_capture_state

def reset_capture_state():
    """重置全局 CaptureState（创建新实例）
    
    用于清空所有捕获数据，通常在开始新一轮捕获前调用。
    """
    global _global_capture_state
    _global_capture_state = CaptureState()
```

**关键点**：
- 使用 `Optional[CaptureState]` 类型提示，初始值为 `None`
- `get_capture_state()` 采用懒加载模式（首次调用时创建）
- `reset_capture_state()` 创建新实例而非调用 `clear()`（更彻底的重置）
- 添加清晰的 docstring 说明用途
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'scripts'); from download_drama import get_capture_state, reset_capture_state; s1 = get_capture_state(); s2 = get_capture_state(); assert s1 is s2, 'Not singleton'; reset_capture_state(); s3 = get_capture_state(); assert s1 is not s3, 'Reset failed'; print('✓ Singleton pattern verified')"</automated>
  </verify>
  <done>
- `get_capture_state()` 和 `reset_capture_state()` 函数存在于模块顶部
- 多次调用 `get_capture_state()` 返回同一实例（单例验证）
- `reset_capture_state()` 后返回新实例（重置验证）
  </done>
</task>

<task type="auto">
  <name>Task 2: 移除 clear_state_fn 回调参数</name>
  <files>scripts/download_drama.py</files>
  <action>
修改以下三个函数签名和实现，移除 `clear_state_fn` 参数，改为直接调用 `reset_capture_state()`：

**1. `_try_start_episode_on_drama_page` 函数（约第 1206 行）**

修改前：
```python
def _try_start_episode_on_drama_page(
    ep_num: int,
    clear_state_fn: Callable[[], None]
) -> bool:
    # ... 函数内部调用 clear_state_fn()
```

修改后：
```python
def _try_start_episode_on_drama_page(
    ep_num: int,
    state: Optional[CaptureState] = None  # 测试时可注入
) -> bool:
    """尝试从剧集页面启动指定集数
    
    Args:
        ep_num: 目标集数
        state: 可选的 CaptureState 实例（测试时注入，生产环境使用全局单例）
    
    Returns:
        bool: 是否成功启动
    """
    if state is None:
        state = get_capture_state()
    
    # 函数内部原本调用 clear_state_fn() 的地方，改为：
    reset_capture_state()
    # ... 其余逻辑保持不变
```

**2. `search_drama_in_app` 函数（约第 1322 行）**

修改前：
```python
def search_drama_in_app(
    drama_name: str,
    clear_state_fn: Callable[[], None]
) -> bool:
    # ... 函数内部调用 clear_state_fn()
```

修改后：
```python
def search_drama_in_app(
    drama_name: str,
    state: Optional[CaptureState] = None  # 测试时可注入
) -> bool:
    """在 App 内搜索短剧并打开
    
    Args:
        drama_name: 短剧名称
        state: 可选的 CaptureState 实例（测试时注入，生产环境使用全局单例）
    
    Returns:
        bool: 是否成功打开短剧
    """
    if state is None:
        state = get_capture_state()
    
    # 函数内部原本调用 clear_state_fn() 的地方，改为：
    reset_capture_state()
    # ... 其余逻辑保持不变
```

**3. 更新三个调用点（约第 1735, 2006, 2113 行）**

查找所有调用这两个函数的地方，移除 `clear_state_fn=state.clear` 参数：

修改前：
```python
_try_start_episode_on_drama_page(ep_num, clear_state_fn=state.clear)
search_drama_in_app(drama_name, clear_state_fn=state.clear)
```

修改后：
```python
_try_start_episode_on_drama_page(ep_num)
search_drama_in_app(drama_name)
```

**关键点**：
- 保留 `state` 参数用于测试注入（默认值 `None`）
- 生产环境自动使用全局单例
- 所有 `clear_state_fn()` 调用改为 `reset_capture_state()`
- 更新 docstring 说明新的参数用途
  </action>
  <verify>
    <automated>grep -n "clear_state_fn" scripts/download_drama.py && echo "❌ Found clear_state_fn references" || echo "✓ All clear_state_fn references removed"</automated>
  </verify>
  <done>
- `_try_start_episode_on_drama_page` 和 `search_drama_in_app` 签名中无 `clear_state_fn` 参数
- 所有调用点移除了 `clear_state_fn=state.clear` 参数
- 函数内部使用 `reset_capture_state()` 替代回调
- grep 搜索确认代码中无 `clear_state_fn` 残留
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 main() 函数使用全局状态</name>
  <files>scripts/download_drama.py</files>
  <action>
修改 `main()` 函数（约第 2021 行开始），在函数开头初始化全局状态：

修改前：
```python
def main():
    # ... 参数解析
    state = CaptureState()  # 局部变量
    # ... 使用 state
```

修改后：
```python
def main():
    # ... 参数解析
    
    # 初始化全局状态
    reset_capture_state()
    state = get_capture_state()  # 获取全局单例引用
    
    # ... 其余逻辑保持不变，继续使用 state 变量
```

**关键点**：
- 在 `main()` 开头调用 `reset_capture_state()` 确保干净的初始状态
- 保留 `state` 局部变量引用，使现有代码无需修改
- 这样 `main()` 内部和模块级函数访问的是同一个实例
  </action>
  <verify>
    <automated>grep -A 5 "^def main" scripts/download_drama.py | grep -q "reset_capture_state()" && grep -A 5 "^def main" scripts/download_drama.py | grep -q "get_capture_state()" && echo "✓ main() uses global state" || echo "❌ main() not updated"</automated>
  </verify>
  <done>
- `main()` 函数开头调用 `reset_capture_state()`
- `main()` 函数通过 `get_capture_state()` 获取状态引用
- 现有代码中的 `state` 变量继续正常工作
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模块边界 | 全局状态可被任意模块访问，需确保线程安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | _global_capture_state | accept | 当前为单线程设计，无并发修改风险；未来多线程需加锁 |
| T-01-02 | Information Disclosure | CaptureState | accept | 状态仅在进程内存中，无持久化敏感数据 |
</threat_model>

<verification>
**单元测试验证**：
```bash
python -c "
import sys
sys.path.insert(0, 'scripts')
from download_drama import get_capture_state, reset_capture_state

# 测试单例模式
s1 = get_capture_state()
s2 = get_capture_state()
assert s1 is s2, 'Singleton failed'

# 测试重置
reset_capture_state()
s3 = get_capture_state()
assert s1 is not s3, 'Reset failed'

print('✓ All singleton tests passed')
"
```

**代码审查验证**：
```bash
# 确认无 clear_state_fn 残留
grep -n "clear_state_fn" scripts/download_drama.py

# 确认新函数存在
grep -n "def get_capture_state" scripts/download_drama.py
grep -n "def reset_capture_state" scripts/download_drama.py
```

**功能回归验证**：
```bash
# 运行现有测试套件
pytest tests/test_download_drama.py -v
```
</verification>

<success_criteria>
- [ ] `get_capture_state()` 和 `reset_capture_state()` 函数存在且通过单例测试
- [ ] `_try_start_episode_on_drama_page` 和 `search_drama_in_app` 签名中无 `clear_state_fn` 参数
- [ ] 所有调用点移除了 `clear_state_fn=state.clear` 参数
- [ ] `main()` 函数使用 `reset_capture_state()` 和 `get_capture_state()`
- [ ] grep 搜索确认代码中无 `clear_state_fn` 残留
- [ ] 现有测试套件全部通过（无破坏性变更）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-core-stability/01-01-SUMMARY.md`，包含：
- 修改的函数列表和行号
- 单例模式验证结果
- 回归测试结果
- 向后兼容性确认
</output>
