---
phase: 01-core-stability
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - scripts/download_drama.py
  - scripts/drama_download_common.py
autonomous: true
requirements:
  - STABILITY-03

must_haves:
  truths:
    - 选集后等待 UI 更新完成再读取 Hook 数据
    - UI 集数与预期不匹配时自动重试
    - 两阶段模式消除 UI lag 导致的内容错位
  artifacts:
    - path: "scripts/download_drama.py"
      provides: "wait_for_ui_stable() 函数和两阶段下载逻辑"
      min_lines: 2173
      exports: ["wait_for_ui_stable"]
    - path: "scripts/drama_download_common.py"
      provides: "detect_ui_context_from_device() 增强版"
      min_lines: 500
  key_links:
    - from: "download_and_decrypt"
      to: "wait_for_ui_stable"
      via: "选集后轮询 UI 直到集数匹配"
      pattern: "wait_for_ui_stable\\(expected_ep=ep_num"
    - from: "wait_for_ui_stable"
      to: "detect_ui_context_from_device"
      via: "轮询 UI 状态"
      pattern: "detect_ui_context_from_device\\(\\)"
---

<objective>
实现两阶段下载模式：先确认 UI 稳定（集数匹配）再读取 Hook 数据，消除 UI lag 导致的内容错位问题。

**Purpose**: 解决 CONCERNS.md 中记录的 UI lag bug（第 50-67 行），通过 UI 稳定性检查确保文件名与内容一致。

**Output**: 
- `wait_for_ui_stable()` 函数：轮询 UI 直到集数匹配或超时
- `download_and_decrypt()` 增加两阶段逻辑：选集 → 等待 UI 稳定 → 读取 Hook 数据
- 移除 `should_accept_out_of_order_episode` 的自动覆盖逻辑
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-stability/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@scripts/download_drama.py
@scripts/drama_download_common.py

# 依赖前序计划的成果
# 01-01-PLAN: 提供 get_capture_state() 和 reset_capture_state()
# 01-02-PLAN: 提供带时间戳的 VideoRef 数据结构
</context>

<interfaces>
<!-- detect_ui_context_from_device 函数（将被调用） -->

从 scripts/drama_download_common.py（约第 100-200 行）：
```python
def detect_ui_context_from_device() -> Optional[UIContext]:
    """从设备 UI 解析剧名、集数、总集数
    
    Returns:
        UIContext: 包含 title, current_episode, total_episodes
        None: 解析失败
    """
```

<!-- download_and_decrypt 函数（将增加两阶段逻辑） -->

从 scripts/download_drama.py（约第 1750-1900 行）：
```python
def download_and_decrypt(ep_num: int):
    """下载并解密指定集数
    
    当前流程：
    1. 选集（通过 select_episode_from_ui）
    2. 立即读取 Hook 数据（可能是旧数据）
    3. 下载和解密
    
    将改为：
    1. 选集
    2. 等待 UI 稳定（wait_for_ui_stable）
    3. 读取 Hook 数据
    4. 下载和解密
    """
```

<!-- resolve_actual_episode 函数（将移除自动覆盖逻辑） -->

从 scripts/download_drama.py（约第 1750-1850 行）：
```python
def resolve_actual_episode(...):
    """解析实际集数，处理 UI 与预期不匹配的情况
    
    当前包含 should_accept_out_of_order_episode 自动覆盖逻辑
    将改为仅记录警告，不自动覆盖
    """
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 wait_for_ui_stable 函数</name>
  <files>scripts/download_drama.py</files>
  <action>
在 `scripts/download_drama.py` 中（约第 1700 行，`download_and_decrypt` 函数之前），添加新函数 `wait_for_ui_stable()`：

```python
def wait_for_ui_stable(
    expected_ep: int,
    timeout: float = 10.0,
    poll_interval: float = 0.5
) -> bool:
    """等待 UI 稳定（集数匹配预期）
    
    用于解决 UI lag 问题：选集后 UI 可能延迟更新，导致读取到旧的集数信息。
    此函数轮询 UI 直到显示的集数与预期一致，或超时。
    
    Args:
        expected_ep: 预期的集数
        timeout: 超时时间（秒）
        poll_interval: 轮询间隔（秒）
    
    Returns:
        bool: True 表示 UI 已稳定且集数匹配，False 表示超时或解析失败
    """
    from scripts.drama_download_common import detect_ui_context_from_device
    
    start_time = time.time()
    attempts = 0
    
    while time.time() - start_time < timeout:
        attempts += 1
        
        # 解析当前 UI 状态
        ui_ctx = detect_ui_context_from_device()
        
        if ui_ctx is None:
            logger.debug(f"[UI稳定性] 第 {attempts} 次尝试：UI 解析失败，继续等待...")
            time.sleep(poll_interval)
            continue
        
        current_ep = ui_ctx.current_episode
        
        if current_ep == expected_ep:
            elapsed = time.time() - start_time
            logger.info(
                f"[UI稳定性] ✓ UI 已稳定：当前集数 {current_ep} 匹配预期 {expected_ep} "
                f"（耗时 {elapsed:.1f}s，{attempts} 次尝试）"
            )
            return True
        else:
            logger.debug(
                f"[UI稳定性] 第 {attempts} 次尝试：当前集数 {current_ep}，"
                f"预期 {expected_ep}，继续等待..."
            )
            time.sleep(poll_interval)
    
    # 超时
    logger.warning(
        f"[UI稳定性] ✗ 超时：UI 集数未匹配预期 {expected_ep} "
        f"（超时 {timeout}s，{attempts} 次尝试）"
    )
    return False
```

**关键点**：
- 使用轮询模式（while 循环 + sleep）而非固定延迟
- 超时时间默认 10 秒（可调整）
- 轮询间隔 0.5 秒（平衡响应速度和 CPU 占用）
- 详细的日志记录（debug 级别显示每次尝试，info 级别显示成功）
- 返回布尔值表示是否成功
  </action>
  <verify>
    <automated>grep -n "def wait_for_ui_stable" scripts/download_drama.py && echo "✓ wait_for_ui_stable function exists" || echo "❌ Function not found"</automated>
  </verify>
  <done>
- `wait_for_ui_stable()` 函数存在于 `download_and_decrypt` 之前
- 函数签名包含 `expected_ep`, `timeout`, `poll_interval` 参数
- 函数内部使用 while 循环轮询 UI
- 返回布尔值表示成功或超时
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 download_and_decrypt 中集成两阶段模式</name>
  <files>scripts/download_drama.py</files>
  <action>
修改 `download_and_decrypt` 函数（约第 1750-1900 行），在选集后增加 UI 稳定性检查：

**查找选集逻辑（可能是 `select_episode_from_ui` 调用）**

修改前：
```python
def download_and_decrypt(ep_num: int):
    state = get_capture_state()
    
    # 1. 选集
    if not select_episode_from_ui(ep_num):
        logger.error(f"[下载] 选集失败: EP{ep_num}")
        return False
    
    # 2. 立即读取 Hook 数据（问题：可能是旧数据）
    time.sleep(2)  # 硬编码延迟
    
    # 3. 过滤时间戳（来自 01-02-PLAN）
    now = time.time()
    recent_refs = [r for r in state.video_refs if now - r.timestamp < 5.0]
    
    if not recent_refs:
        logger.warning("[下载] 无最近的 Hook 数据")
        return False
    
    vid_ref = max(recent_refs, key=lambda r: r.timestamp)
    # ... 下载和解密
```

修改后：
```python
def download_and_decrypt(ep_num: int):
    state = get_capture_state()
    
    # 阶段 1: 选集
    if not select_episode_from_ui(ep_num):
        logger.error(f"[下载] 选集失败: EP{ep_num}")
        return False
    
    # 阶段 2: 等待 UI 稳定（新增）
    logger.info(f"[下载] 等待 UI 更新到 EP{ep_num}...")
    if not wait_for_ui_stable(expected_ep=ep_num, timeout=10.0):
        logger.error(
            f"[下载] UI 未稳定到 EP{ep_num}，可能选集失败或 UI 响应慢，"
            "清空 state 并重试"
        )
        reset_capture_state()  # 清空可能的脏数据
        return False
    
    # 阶段 3: 读取 Hook 数据（此时 UI 已确认是正确集数）
    # 移除硬编码的 time.sleep(2)，因为 wait_for_ui_stable 已经等待
    
    # 过滤时间戳（来自 01-02-PLAN）
    now = time.time()
    recent_refs = [r for r in state.video_refs if now - r.timestamp < 5.0]
    
    if not recent_refs:
        logger.warning(
            f"[下载] 无最近的 Hook 数据（UI 已稳定但 Hook 未触发），"
            "可能需要手动播放或等待预加载"
        )
        return False
    
    vid_ref = max(recent_refs, key=lambda r: r.timestamp)
    
    # 阶段 4: 下载和解密（保持不变）
    # ... 现有逻辑
```

**关键点**：
- 在选集和读取 Hook 数据之间插入 `wait_for_ui_stable()` 调用
- 移除硬编码的 `time.sleep(2)`（被 `wait_for_ui_stable` 替代）
- UI 未稳定时清空 state 并返回 `False`（触发外层重试）
- 添加清晰的阶段注释（阶段 1/2/3/4）
- 日志说明当前处于哪个阶段
  </action>
  <verify>
    <automated>grep -A 30 "def download_and_decrypt" scripts/download_drama.py | grep -q "wait_for_ui_stable" && echo "✓ Two-phase mode integrated" || echo "❌ Integration not found"</automated>
  </verify>
  <done>
- `download_and_decrypt` 函数在选集后调用 `wait_for_ui_stable()`
- UI 未稳定时清空 state 并返回 `False`
- 移除了硬编码的 `time.sleep(2)`
- 添加了阶段注释和日志
  </done>
</task>

<task type="auto">
  <name>Task 3: 移除 should_accept_out_of_order_episode 自动覆盖</name>
  <files>scripts/download_drama.py</files>
  <action>
查找 `resolve_actual_episode` 函数或 `should_accept_out_of_order_episode` 相关逻辑（约第 1750-1850 行），移除自动覆盖 `ep_num` 的行为：

**查找类似以下的代码**

修改前：
```python
def resolve_actual_episode(expected_ep: int, ui_ep: int, ...) -> int:
    """解析实际集数"""
    
    if ui_ep != expected_ep:
        # 检查是否应该接受乱序集数（跳集填补）
        if should_accept_out_of_order_episode(ui_ep, expected_ep, ...):
            logger.warning(
                f"[集数解析] 接受乱序集数：预期 EP{expected_ep}，实际 EP{ui_ep}"
            )
            return ui_ep  # 自动覆盖（问题：可能导致内容错位）
    
    return expected_ep
```

修改后：
```python
def resolve_actual_episode(expected_ep: int, ui_ep: int, ...) -> int:
    """解析实际集数
    
    注意：不再自动覆盖 ep_num，仅记录警告。
    两阶段模式已确保 UI 稳定，不应出现集数不匹配。
    """
    
    if ui_ep != expected_ep:
        # 记录警告但不自动覆盖
        logger.warning(
            f"[集数解析] ⚠ UI 集数不匹配：预期 EP{expected_ep}，UI 显示 EP{ui_ep}。"
            f"这不应该发生（两阶段模式已确保 UI 稳定）。"
            f"保持使用预期集数 {expected_ep}。"
        )
        
        # 可选：检查是否是跳集场景（用户手动跳过某些集）
        if should_accept_out_of_order_episode(ui_ep, expected_ep, ...):
            logger.info(
                f"[集数解析] 检测到跳集场景（预期 {expected_ep} → UI {ui_ep}），"
                "但不自动覆盖。如需下载 EP{ui_ep}，请手动指定 -e {ui_ep}。"
            )
    
    return expected_ep  # 始终返回预期集数
```

**如果找不到 `resolve_actual_episode` 函数**

查找直接使用 `should_accept_out_of_order_episode` 的地方，注释掉自动覆盖逻辑：

```python
# 修改前
if should_accept_out_of_order_episode(...):
    ep_num = ui_ep  # 自动覆盖

# 修改后
if should_accept_out_of_order_episode(...):
    logger.warning(
        f"检测到跳集场景，但不自动覆盖 ep_num。"
        f"两阶段模式已确保 UI 稳定，保持使用预期集数 {ep_num}。"
    )
    # ep_num = ui_ep  # 已禁用自动覆盖
```

**关键点**：
- 保留 `should_accept_out_of_order_episode` 检测逻辑（用于日志）
- 移除自动覆盖 `ep_num` 的行为
- 添加清晰的注释说明为何禁用
- 日志提示用户如何手动处理跳集场景
  </action>
  <verify>
    <automated>grep -n "should_accept_out_of_order_episode" scripts/download_drama.py | head -5 && echo "Checking for auto-override removal..." || echo "Function not found"</automated>
  </verify>
  <done>
- `resolve_actual_episode` 或相关逻辑不再自动覆盖 `ep_num`
- 保留检测逻辑但仅记录警告
- 添加了注释说明禁用原因
- 日志提示用户手动处理跳集
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Python | UI XML 解析结果可信（来自 uiautomator） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | wait_for_ui_stable 超时 | mitigate | 设置合理超时（10 秒），超时后返回 False 触发重试 |
| T-01-06 | Information Disclosure | UI 解析失败 | accept | 解析失败仅影响当前集下载，不泄露敏感信息 |
</threat_model>

<verification>
**单元测试验证**：
```bash
# 测试 wait_for_ui_stable 函数存在
python -c "
import sys
sys.path.insert(0, 'scripts')
from download_drama import wait_for_ui_stable
import inspect
sig = inspect.signature(wait_for_ui_stable)
assert 'expected_ep' in sig.parameters, 'Missing expected_ep parameter'
assert 'timeout' in sig.parameters, 'Missing timeout parameter'
print('✓ wait_for_ui_stable signature verified')
"
```

**代码审查验证**：
```bash
# 确认 wait_for_ui_stable 函数存在
grep -n "def wait_for_ui_stable" scripts/download_drama.py

# 确认 download_and_decrypt 调用 wait_for_ui_stable
grep -A 30 "def download_and_decrypt" scripts/download_drama.py | grep "wait_for_ui_stable"

# 确认移除了自动覆盖逻辑
grep -B 5 -A 5 "should_accept_out_of_order_episode" scripts/download_drama.py
```

**功能回归验证**：
```bash
# 运行现有测试套件
pytest tests/test_download_drama.py -v
```
</verification>

<success_criteria>
- [ ] `wait_for_ui_stable()` 函数存在且签名正确
- [ ] 函数内部使用 while 循环轮询 UI
- [ ] `download_and_decrypt` 在选集后调用 `wait_for_ui_stable()`
- [ ] UI 未稳定时清空 state 并返回 `False`
- [ ] 移除了硬编码的 `time.sleep(2)`
- [ ] `should_accept_out_of_order_episode` 不再自动覆盖 `ep_num`
- [ ] 现有测试套件全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-core-stability/01-03-SUMMARY.md`，包含：
- 新增的 `wait_for_ui_stable()` 函数签名和实现
- `download_and_decrypt` 两阶段模式集成点
- 移除的自动覆盖逻辑位置
- 回归测试结果
</output>
