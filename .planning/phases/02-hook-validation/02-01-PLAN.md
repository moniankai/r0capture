---
phase: 02-hook-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frida_hooks/ttengine_all.js
  - frida_hooks/aes_hook.js
  - scripts/download_drama.py
autonomous: true
requirements: [HOOK-01, HOOK-02]
must_haves:
  truths:
    - "Hook 数据包含 episode_number 字段（Java 层和 Native 层）"
    - "episode_number 为 null 时不影响现有功能（向后兼容）"
    - "Python 端能正确解析 Hook 消息中的 episode_number"
  artifacts:
    - path: "frida_hooks/ttengine_all.js"
      provides: "Java Hook 增加 episode_number 提取逻辑"
      min_lines: 200
    - path: "frida_hooks/aes_hook.js"
      provides: "Native Hook 增加 episode_number 关联"
      min_lines: 120
    - path: "scripts/download_drama.py"
      provides: "VideoRef 和 AESKey 数据类增加 episode_number 字段"
      contains: "episode_number: Optional[int]"
  key_links:
    - from: "frida_hooks/ttengine_all.js"
      to: "scripts/download_drama.py"
      via: "send() 消息传递"
      pattern: "episode_number.*send"
    - from: "VideoRef.episode_number"
      to: "download_and_decrypt"
      via: "数据类字段访问"
      pattern: "r\\.episode_number"
---

<objective>
为 Hook 端增加 episode_number 字段，建立 Hook 数据与集数的关联。

Purpose: 消除时序依赖，为精确匹配奠定基础。当目标集和 preload 集的 Hook 同时触发时，Python 端能根据 episode_number 区分数据来源。

Output: 
- Hook 脚本增强（Java + Native）
- Python 数据结构更新
- 向后兼容的回退机制
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-stability/01-01-SUMMARY.md
@.planning/phases/01-core-stability/01-02-SUMMARY.md
@.planning/phases/02-hook-validation/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Java Hook 增加 episode_number 提取</name>
  <files>frida_hooks/ttengine_all.js</files>
  <action>
在 `COMBINED_HOOK` 的 Java Hook 部分（`scripts/download_drama.py` 第 111-180 行）增加 episode_number 提取逻辑：

1. **提取策略**（按优先级）：
   - 方案 A：从 `VideoModel` 对象的字段提取（`mEpisodeNumber`、`mIndex`、`mOrder` 等）
   - 方案 B：从 UI 层提取（通过 `ActivityThread.currentActivity()` 获取当前 Activity，解析 UI 元素）
   - 方案 C：返回 `null`（回退到 Python 端时序选择）

2. **实现细节**：
   ```javascript
   function extractEpisodeNumber(videoModel) {
       // 方案 A: 从 VideoModel 字段提取
       try {
           var episodeFields = ["mEpisodeNumber", "mIndex", "mOrder", "mEpisode", "episodeNum"];
           for (var i = 0; i < episodeFields.length; i++) {
               var field = findFieldInHierarchy(videoModel, episodeFields[i]);
               if (field) {
                   var val = field.get(videoModel);
                   if (val !== null) {
                       var num = parseInt(val.toString());
                       if (!isNaN(num) && num > 0) {
                           return num;
                       }
                   }
               }
           }
       } catch (e) {}
       
       // 方案 B: 从 UI 层提取（可选，复杂度高）
       // 暂时跳过，留待后续优化
       
       // 方案 C: 返回 null
       return null;
   }
   ```

3. **修改 setVideoModel Hook**：
   在 `send({t: "video_ref", ...})` 消息中增加 `episode_number` 字段：
   ```javascript
   var episodeNumber = extractEpisodeNumber(model);
   send({
       t: "video_ref",
       video_id: vid,
       duration: dur,
       data: dump,
       episode_number: episodeNumber,  // 新增
       timestamp: Date.now()
   });
   ```

4. **日志输出**：
   ```javascript
   if (episodeNumber !== null) {
       console.log("[Hook] Episode number: " + episodeNumber);
   }
   ```

**注意**：
- 使用现有的 `findFieldInHierarchy()` 函数（第 139-150 行）
- 保持向后兼容：`episode_number` 为 `null` 时不影响现有逻辑
- 不要修改 `video_model` 消息（第 177 行），仅修改 `video_ref` 消息
  </action>
  <verify>
    <automated>
# 验证 Hook 脚本语法
node -c frida_hooks/ttengine_all.js 2>&1 || echo "Syntax check failed"

# 验证关键字段存在
grep -n "episode_number" scripts/download_drama.py | head -5
grep -n "extractEpisodeNumber" scripts/download_drama.py | head -3
    </automated>
  </verify>
  <done>
- `extractEpisodeNumber()` 函数存在于 COMBINED_HOOK 中
- `video_ref` 消息包含 `episode_number` 字段
- Hook 脚本语法正确（无 JavaScript 错误）
  </done>
</task>

<task type="auto">
  <name>Task 2: Native Hook 增加 episode_number 关联</name>
  <files>frida_hooks/aes_hook.js, scripts/download_drama.py</files>
  <action>
在 `COMBINED_HOOK` 的 Native Hook 部分（`scripts/download_drama.py` 第 182-218 行）增加 episode_number 关联逻辑：

1. **策略**：Native Hook 无法直接访问 Java 层状态，因此使用全局变量缓存最近的 episode_number

2. **实现细节**：
   ```javascript
   // 在 COMBINED_HOOK 顶部（第 91 行附近）增加全局变量
   var lastEpisodeNumber = null;  // 缓存最近的 episode_number
   
   // 在 Java Hook 的 setVideoModel 中更新全局变量
   Java.perform(function() {
       // ... 现有代码 ...
       
       Engine.setVideoModel.implementation = function(model) {
           // ... 现有代码 ...
           var episodeNumber = extractEpisodeNumber(model);
           lastEpisodeNumber = episodeNumber;  // 更新全局缓存
           
           send({
               t: "video_ref",
               // ... 现有字段 ...
               episode_number: episodeNumber
           });
           
           return this.setVideoModel(model);
       };
   });
   
   // 在 Native Hook 的 av_aes_init 中使用缓存的 episode_number
   function hookAesInit() {
       // ... 现有代码 ...
       Interceptor.attach(aesAddr, {
           onEnter: function(args) {
               // ... 现有代码 ...
           },
           onLeave: function(retval) {
               var bits = this.bits;
               var dec = this.dec;
               if (bits === 128 || bits === 256) {
                   send({
                       t: "AES_KEY",
                       bits: bits,
                       dec: dec,
                       key: bh(this.key, bits / 8),
                       episode_number: lastEpisodeNumber,  // 新增
                       timestamp: Date.now()
                   });
               }
           }
       });
   }
   ```

3. **日志输出**：
   ```javascript
   if (lastEpisodeNumber !== null) {
       console.log("[AES Hook] Associated with episode: " + lastEpisodeNumber);
   }
   ```

**注意**：
- 全局变量 `lastEpisodeNumber` 在 Java Hook 触发时更新
- Native Hook 使用缓存值，假设 AES 密钥在 setVideoModel 之后不久触发
- 这是一个近似关联，可能存在时序问题，但优于完全无关联
  </action>
  <verify>
    <automated>
# 验证 Hook 脚本语法
node -c <(grep -A 500 'COMBINED_HOOK = r"""' scripts/download_drama.py | sed '1d' | sed 's/"""$//')

# 验证关键字段存在
grep -n "lastEpisodeNumber" scripts/download_drama.py | head -3
grep -n "episode_number.*AES_KEY" scripts/download_drama.py | head -2
    </automated>
  </verify>
  <done>
- `lastEpisodeNumber` 全局变量存在于 COMBINED_HOOK 中
- `AES_KEY` 消息包含 `episode_number` 字段
- Java Hook 更新 `lastEpisodeNumber` 缓存
  </done>
</task>

<task type="auto">
  <name>Task 3: Python 数据结构更新</name>
  <files>scripts/download_drama.py</files>
  <action>
更新 `VideoRef` 和 `AESKey` 数据类，增加 `episode_number` 字段：

1. **修改 VideoRef 数据类**（第 71-78 行）：
   ```python
   @dataclass
   class VideoRef:
       """视频引用数据（从 Java Hook 捕获）"""
       video_id: str
       duration: int
       raw_data: Dict[str, Any]
       timestamp: float = 0.0  # 捕获时间戳（Unix 时间）
       episode_number: Optional[int] = None  # 新增：集数（从 Hook 提取）
       context: Dict[str, Any] = field(default_factory=dict)  # 上下文信息（预留）
   ```

2. **修改 AESKey 数据类**（第 81-87 行）：
   ```python
   @dataclass
   class AESKey:
       """AES 解密密钥（从 Native Hook 捕获）"""
       key_hex: str
       bits: int
       timestamp: float = 0.0  # 捕获时间戳（Unix 时间）
       episode_number: Optional[int] = None  # 新增：关联的集数
       context: Dict[str, Any] = field(default_factory=dict)  # 上下文信息（预留）
   ```

3. **修改 on_message 回调**（第 1662 行附近）：
   在处理 `video_ref` 消息时提取 `episode_number`：
   ```python
   elif t == "video_ref":
       d = p["data"]
       vid = p.get("video_id", "")
       dur = p.get("duration", 0)
       ep_num = p.get("episode_number")  # 新增
       
       if vid:
           ref = VideoRef(
               video_id=vid,
               duration=dur,
               raw_data=d,
               timestamp=time.time(),
               episode_number=ep_num  # 新增
           )
           with state.lock:
               state.video_refs.append(ref)
           
           if ep_num is not None:
               logger.info(f"[捕获] video_id={vid[:8]}... episode_number={ep_num}")
           else:
               logger.info(f"[捕获] video_id={vid[:8]}... (episode_number 未提取)")
   ```

4. **修改 AES_KEY 消息处理**（第 1700 行附近）：
   ```python
   elif t == "AES_KEY":
       key_hex = p.get("key", "")
       bits = p.get("bits", 0)
       ep_num = p.get("episode_number")  # 新增
       
       if key_hex and bits in (128, 256):
           aes_key = AESKey(
               key_hex=key_hex,
               bits=bits,
               timestamp=time.time(),
               episode_number=ep_num  # 新增
           )
           with state.lock:
               if not any(k.key_hex == key_hex for k in state.aes_keys):
                   state.aes_keys.append(aes_key)
                   
                   if ep_num is not None:
                       logger.info(f"[捕获] AES-{bits} 密钥 (episode_number={ep_num})")
                   else:
                       logger.info(f"[捕获] AES-{bits} 密钥 (episode_number 未关联)")
   ```

**注意**：
- `episode_number` 默认值为 `None`，保持向后兼容
- 日志输出区分 `episode_number` 是否可用
- 不修改现有的数据选择逻辑（留待 Plan 02-02）
  </action>
  <verify>
    <automated>
# 验证数据类定义
grep -A 5 "class VideoRef:" scripts/download_drama.py | grep "episode_number"
grep -A 5 "class AESKey:" scripts/download_drama.py | grep "episode_number"

# 验证 on_message 处理
grep -n "episode_number.*video_ref" scripts/download_drama.py
grep -n "episode_number.*AES_KEY" scripts/download_drama.py

# 运行现有测试（确保向后兼容）
pytest tests/test_download_drama.py -v
    </automated>
  </verify>
  <done>
- `VideoRef` 和 `AESKey` 包含 `episode_number: Optional[int]` 字段
- `on_message` 回调正确解析 `episode_number`
- 所有现有测试通过（47/47）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frida Hook → Python | Hook 脚本发送的消息可能被篡改或伪造 |
| VideoModel 字段 → episode_number | 字段名可能在 App 更新后变化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | episode_number 字段 | accept | Hook 端提取失败时返回 null，Python 端回退到时序选择（Phase 1 逻辑） |
| T-02-02 | Information Disclosure | episode_number 日志 | accept | 集数信息非敏感数据，日志输出可接受 |
| T-02-03 | Denial of Service | 错误的 episode_number | mitigate | Python 端校验 episode_number 范围（1-9999），超出范围视为 null |
</threat_model>

<verification>
## 单元测试

1. **数据类字段验证**：
   ```python
   ref = VideoRef(video_id="test", duration=100, raw_data={}, episode_number=5)
   assert ref.episode_number == 5
   
   key = AESKey(key_hex="abc123", bits=128, episode_number=5)
   assert key.episode_number == 5
   ```

2. **向后兼容性验证**：
   ```python
   ref = VideoRef(video_id="test", duration=100, raw_data={})
   assert ref.episode_number is None  # 默认值
   ```

## 集成测试

1. **Hook 消息解析**：
   - 模拟 `video_ref` 消息包含 `episode_number`
   - 验证 `VideoRef` 对象正确创建
   - 验证日志输出包含 `episode_number=N`

2. **回退机制验证**：
   - 模拟 `video_ref` 消息不包含 `episode_number`
   - 验证 `VideoRef.episode_number` 为 `None`
   - 验证日志输出包含 "episode_number 未提取"

## 回归测试

运行 Phase 1 的 47 个测试，确保无破坏性变更。
</verification>

<success_criteria>
1. Hook 脚本包含 `extractEpisodeNumber()` 函数
2. `video_ref` 和 `AES_KEY` 消息包含 `episode_number` 字段
3. `VideoRef` 和 `AESKey` 数据类包含 `episode_number: Optional[int]` 字段
4. `on_message` 回调正确解析 `episode_number`
5. 所有现有测试通过（47/47）
6. 日志输出区分 `episode_number` 是否可用
</success_criteria>

<output>
After completion, create `.planning/phases/02-hook-validation/02-01-SUMMARY.md`
</output>
