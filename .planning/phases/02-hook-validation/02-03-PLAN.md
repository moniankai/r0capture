---
phase: 02-hook-validation
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - tests/test_download_drama.py
  - scripts/download_drama.py
autonomous: false
requirements: [HOOK-04]
must_haves:
  truths:
    - "在真实设备上验证 episode_number 提取成功率 > 80%"
    - "多集预加载场景下无内容错位（AES 密钥与 video_id 正确关联）"
    - "回归测试全部通过（51 个测试）"
  artifacts:
    - path: "tests/test_download_drama.py"
      provides: "完整的测试覆盖（51 个测试）"
      min_lines: 800
    - path: ".planning/phases/02-hook-validation/02-03-SUMMARY.md"
      provides: "Phase 2 完整总结和验证报告"
      min_lines: 100
  key_links:
    - from: "真实设备测试"
      to: "episode_number 提取成功率"
      via: "match_stats 统计"
      pattern: "exact_match.*%"
---

<objective>
集成测试和真实设备验证，确保 Phase 2 的所有改进在生产环境中正常工作。

Purpose: 验证 Hook 端 episode_number 提取的可靠性，确认精确匹配逻辑消除了数据竞争问题。

Output: 
- 真实设备测试报告
- 回归测试验证
- Phase 2 完整总结
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hook-validation/02-CONTEXT.md
@.planning/phases/02-hook-validation/02-01-PLAN.md
@.planning/phases/02-hook-validation/02-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 回归测试验证</name>
  <files>tests/test_download_drama.py</files>
  <action>
运行完整的测试套件，确保 Phase 2 的修改无破坏性变更：

1. **运行所有单元测试**：
   ```bash
   pytest tests/test_download_drama.py -v --tb=short
   ```

2. **验证测试覆盖**：
   - Phase 1 的 47 个测试（UI 解析、会话校验、文件名生成等）
   - Phase 2 新增的 4 个测试（episode_number 匹配逻辑）
   - 总计 51 个测试

3. **检查测试输出**：
   - 所有测试通过（51/51）
   - 无警告或错误
   - 执行时间 < 1 秒（单元测试应快速）

4. **如果测试失败**：
   - 记录失败的测试用例
   - 分析失败原因（代码 bug vs 测试 bug）
   - 修复问题并重新运行

**预期结果**：
```
======================== test session starts ========================
collected 51 items

tests/test_download_drama.py::ParseUiContextTests::test_parse_basic ... PASSED
tests/test_download_drama.py::ParseUiContextTests::test_parse_with_total ... PASSED
tests/test_download_drama.py::ParseUiContextTests::test_parse_missing_fields ... PASSED
tests/test_download_drama.py::ParseUiContextTests::test_parse_invalid_xml ... PASSED
tests/test_download_drama.py::FileNamingTests::test_sanitize_drama_name ... PASSED
tests/test_download_drama.py::FileNamingTests::test_build_episode_paths ... PASSED
tests/test_download_drama.py::SessionValidationTests::test_first_round ... PASSED
tests/test_download_drama.py::SessionValidationTests::test_title_drift ... PASSED
tests/test_download_drama.py::SessionValidationTests::test_duplicate_video_id ... PASSED
tests/test_download_drama.py::SessionValidationTests::test_episode_not_ascending ... PASSED
tests/test_download_drama.py::SessionValidationTests::test_valid_progression ... PASSED
... (46 more tests)
tests/test_download_drama.py::EpisodeNumberMatchingTests::test_exact_match_success ... PASSED
tests/test_download_drama.py::EpisodeNumberMatchingTests::test_fallback_to_timestamp ... PASSED
tests/test_download_drama.py::EpisodeNumberMatchingTests::test_partial_match_fallback ... PASSED
tests/test_download_drama.py::EpisodeNumberMatchingTests::test_multiple_matches_select_latest ... PASSED

======================== 51 passed in 0.25s ========================
```
  </action>
  <verify>
    <automated>
pytest tests/test_download_drama.py -v --tb=short
    </automated>
  </verify>
  <done>
- 所有 51 个测试通过
- 无警告或错误
- 测试执行时间 < 1 秒
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 2 的 Hook 端增强和 Python 端精确匹配逻辑已完成：
- Hook 脚本增加 episode_number 提取（Java + Native）
- Python 数据结构更新（VideoRef 和 AESKey）
- 精确匹配逻辑（优先使用 episode_number）
- 时序选择回退（兼容 Phase 1）
- 数据选择统计（监控成功率）
  </what-built>
  <how-to-verify>
**真实设备测试步骤**：

1. **准备测试环境**：
   ```bash
   # 确保设备连接
   adb devices
   
   # 确保 Frida Server 运行
   adb shell "ps | grep frida-server"
   ```

2. **测试场景 1：单集下载（验证基本功能）**：
   ```bash
   python scripts/download_drama.py -n "测试短剧" -e 1
   ```
   
   **预期结果**：
   - Hook 成功注入
   - 日志显示 `[捕获] video_id=... episode_number=1` 或 `episode_number 未提取`
   - 日志显示 `[数据选择] ✓ 精确匹配 episode_number=1` 或 `回退到时序选择`
   - 视频下载和解密成功
   - 输出文件：`videos/测试短剧/episode_001_<vid>.mp4`

3. **测试场景 2：批量下载（验证多集场景）**：
   ```bash
   python scripts/download_drama.py -n "测试短剧" -e 1 -b 5
   ```
   
   **预期结果**：
   - 连续下载 5 集
   - 每集日志显示数据选择策略（精确匹配 or 时序选择）
   - 批量结束时输出统计信息：
     ```
     [统计] 数据选择策略分布:
       - 精确匹配: X (Y%)
       - 时序选择: Z
       - 部分匹配: 0
     ```
   - 所有视频文件内容与文件名一致（无错位）

4. **测试场景 3：搜索模式（验证自动化流程）**：
   ```bash
   python scripts/download_drama.py -n "凡人仙葫第一季" --search -e 1 -b 3
   ```
   
   **预期结果**：
   - 自动搜索并打开剧集
   - 连续下载 3 集
   - episode_number 提取成功率 > 80%（从统计信息判断）
   - 无内容错位

5. **验证内容正确性**：
   - 随机抽查 2-3 个视频文件
   - 使用播放器打开，确认内容与文件名一致
   - 检查 `meta_epXXX_<vid>.json` 中的元数据

6. **检查日志文件**：
   ```bash
   # 查看最近的日志
   tail -100 <日志文件>
   
   # 统计 episode_number 提取成功次数
   grep "episode_number=" <日志文件> | grep -v "未提取" | wc -l
   grep "episode_number 未提取" <日志文件> | wc -l
   ```

**成功标准**：
- ✓ 单集下载成功（场景 1）
- ✓ 批量下载成功，无内容错位（场景 2）
- ✓ 搜索模式正常工作（场景 3）
- ✓ episode_number 提取成功率 > 80%（从统计信息或日志判断）
- ✓ 视频内容与文件名一致（随机抽查）

**如果 episode_number 提取成功率 < 80%**：
- 检查 Hook 脚本的字段名是否正确（`mEpisodeNumber`、`mIndex` 等）
- 检查日志中的 `[Model] fields:` 输出，确认 VideoModel 包含哪些字段
- 如果字段名不匹配，需要调整 `extractEpisodeNumber()` 函数
- 即使提取失败，回退机制应确保功能正常（时序选择）
  </how-to-verify>
  <resume-signal>
输入以下内容之一：
- `approved` — 所有测试通过，episode_number 提取成功率 > 80%
- `approved-low-rate` — 测试通过，但 episode_number 提取成功率 < 80%（回退机制工作正常）
- `failed: <原因>` — 测试失败，描述具体问题
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: 创建 Phase 2 总结</name>
  <files>.planning/phases/02-hook-validation/02-03-SUMMARY.md</files>
  <action>
创建 Phase 2 的完整总结文档，记录所有修改、决策和验证结果：

```markdown
---
phase: 02-hook-validation
plan: 03
subsystem: hook-data-validation
tags: [episode-number, precise-matching, integration-test]
dependency_graph:
  requires: [01-01-SUMMARY.md, 01-02-SUMMARY.md]
  provides: [episode-number-matching, hook-data-association]
  affects: [download_drama.py, frida_hooks]
tech_stack:
  added: [episode-number-extraction, precise-matching-logic]
  patterns: [fallback-mechanism, match-statistics]
key_files:
  created: []
  modified:
    - path: frida_hooks/ttengine_all.js
      lines_changed: 30
      description: "增加 extractEpisodeNumber() 函数，从 VideoModel 提取集数"
    - path: scripts/download_drama.py
      lines_changed: 120
      description: "增加 episode_number 字段、精确匹配逻辑、统计功能"
    - path: tests/test_download_drama.py
      lines_changed: 80
      description: "新增 EpisodeNumberMatchingTests 测试类（4 个测试）"
decisions:
  - id: DEC-02-01
    summary: "Hook 端使用字段名枚举策略提取 episode_number"
    rationale: "VideoModel 字段名可能变化，枚举多个候选字段提高成功率"
  - id: DEC-02-02
    summary: "Native Hook 使用全局变量缓存 episode_number"
    rationale: "Native 层无法直接访问 Java 层状态，通过全局变量近似关联"
  - id: DEC-02-03
    summary: "精确匹配失败时强制回退到时序选择"
    rationale: "确保即使 episode_number 提取失败也能继续工作（向后兼容）"
  - id: DEC-02-04
    summary: "增加 match_stats 统计监控成功率"
    rationale: "量化 episode_number 提取的可靠性，便于后续优化"
metrics:
  duration_minutes: <实际执行时间>
  tasks_completed: 3
  files_modified: 3
  lines_added: 230
  lines_removed: 10
  tests_passed: 51
  commits: <提交数量>
  completed_date: 2026-04-15
---

# Phase 02 Plan 03: 集成测试和验证 Summary

**一句话总结**: 通过真实设备测试验证 Hook 端 episode_number 提取和 Python 端精确匹配逻辑，确认消除了多集预加载场景下的数据竞争问题。

## 执行概览

**目标**: 验证 Phase 2 的所有改进在生产环境中正常工作。

**结果**: <根据实际测试结果填写>

## 完成的任务

### Task 1: 回归测试验证
**结果**: <填写测试结果>

### Task 2: 真实设备验证
**测试场景**:
1. 单集下载 — <结果>
2. 批量下载（5 集）— <结果>
3. 搜索模式（3 集）— <结果>

**episode_number 提取成功率**: <X%>

**数据选择策略分布**:
- 精确匹配: <X 次> (<Y%>)
- 时序选择: <Z 次>
- 部分匹配: <W 次>

**内容正确性验证**: <抽查结果>

### Task 3: Phase 2 总结
**文档**: 本文件

## 偏差记录

<记录实际执行与计划的偏差>

## 验证结果

### 回归测试
- 单元测试: 51/51 通过
- 执行时间: <X 秒>

### 真实设备测试
- 单集下载: <通过/失败>
- 批量下载: <通过/失败>
- 搜索模式: <通过/失败>
- episode_number 提取成功率: <X%>

## 向后兼容性

✓ **完全兼容** — 所有现有功能保持不变：
- episode_number 为 null 时自动回退到时序选择
- Phase 1 的时间戳过滤逻辑保持不变
- 所有 Phase 1 测试通过

## 技术决策

### 决策 1: Hook 端字段名枚举策略
**选择**: 枚举多个候选字段名（`mEpisodeNumber`、`mIndex`、`mOrder` 等）

**理由**:
- VideoModel 字段名可能在 App 更新后变化
- 枚举策略提高提取成功率
- 失败时返回 null，不影响功能

### 决策 2: Native Hook 全局变量缓存
**选择**: 使用全局变量 `lastEpisodeNumber` 缓存最近的集数

**理由**:
- Native 层无法直接访问 Java 层状态
- 假设 AES 密钥在 setVideoModel 之后不久触发
- 近似关联优于完全无关联

### 决策 3: 强制回退机制
**选择**: 部分匹配时强制回退到时序选择

**理由**:
- 避免仅有 refs 或仅有 keys 匹配导致的数据不一致
- 确保 video_id 和 AES 密钥始终配对
- 回退机制保证功能可用性

### 决策 4: 统计监控
**选择**: 增加 `match_stats` 统计数据选择策略分布

**理由**:
- 量化 episode_number 提取的可靠性
- 便于识别 Hook 端需要优化的场景
- 为后续改进提供数据支持

## 已知限制

1. **Native Hook 关联不精确**: 使用全局变量缓存，可能在极端时序下关联错误
2. **字段名依赖**: 如果 App 更新后字段名完全变化，需要更新 Hook 脚本
3. **提取成功率**: 当前提取成功率约 <X%>，未达到 100%（但回退机制确保功能可用）

## 下游影响

### 对 Phase 3 的影响
- Phase 3 可以依赖 episode_number 字段进行更精确的错误恢复
- 统计信息可用于识别需要重试的场景

### 对未来扩展的影响
- episode_number 字段为多 App 支持奠定基础（Phase 4）
- 精确匹配逻辑可复用到其他短剧 App

## 文件清单

### 修改的文件
- `frida_hooks/ttengine_all.js` (30 行新增)
  - `extractEpisodeNumber()` 函数
  - `video_ref` 消息增加 `episode_number` 字段
- `scripts/download_drama.py` (120 行新增, 10 行删除)
  - `VideoRef` 和 `AESKey` 增加 `episode_number` 字段
  - `download_and_decrypt` 增加精确匹配逻辑
  - `CaptureState` 增加 `match_stats` 统计
- `tests/test_download_drama.py` (80 行新增)
  - `EpisodeNumberMatchingTests` 测试类（4 个测试）

### 提交记录
<根据实际提交填写>

## Self-Check: PASSED

### 文件存在性检查
```bash
✓ frida_hooks/ttengine_all.js modified
✓ scripts/download_drama.py modified
✓ tests/test_download_drama.py modified
```

### 功能验证
```bash
✓ extractEpisodeNumber() 函数存在
✓ episode_number 字段存在于数据类
✓ 精确匹配逻辑存在
✓ 回退机制存在
✓ 统计功能存在
✓ 所有测试通过 (51/51)
```

## 后续建议

1. **优化字段名枚举**: 根据真实测试结果，调整字段名优先级
2. **增强 Native Hook 关联**: 探索更精确的关联方法（如调用栈回溯）
3. **监控提取成功率**: 在生产环境中持续监控，识别需要优化的场景
4. **文档更新**: 在 README.md 中说明 episode_number 字段和精确匹配逻辑

## 总结

Phase 2 成功实现了 Hook 端 episode_number 提取和 Python 端精确匹配逻辑，消除了多集预加载场景下的数据竞争问题。通过强制回退机制，确保即使 episode_number 提取失败也能继续工作（向后兼容）。真实设备测试验证了功能的可靠性，episode_number 提取成功率达到 <X%>。统计功能为后续优化提供了数据支持。
```

**注意**：
- 根据 Task 2 的实际测试结果填写 `<占位符>` 内容
- 记录所有提交的 commit hash
- 如果测试失败，记录失败原因和修复方案
  </action>
  <verify>
    <automated>
# 验证 SUMMARY 文件存在
test -f .planning/phases/02-hook-validation/02-03-SUMMARY.md && echo "SUMMARY exists"

# 验证文件内容完整性
grep -q "Phase 02 Plan 03" .planning/phases/02-hook-validation/02-03-SUMMARY.md
grep -q "episode_number" .planning/phases/02-hook-validation/02-03-SUMMARY.md
    </automated>
  </verify>
  <done>
- `02-03-SUMMARY.md` 文件存在
- 文件包含完整的 Phase 2 总结
- 所有占位符已根据实际测试结果填写
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 真实设备 → 测试结果 | 设备状态可能影响测试结果（网络、性能等） |
| Hook 提取 → 成功率统计 | 统计数据依赖 Hook 端实现的可靠性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | 真实设备测试 | accept | 测试失败时记录详细日志，便于后续分析 |
| T-02-08 | Information Disclosure | 测试日志 | accept | 日志仅包含测试数据，无敏感信息 |
</threat_model>

<verification>
## 回归测试

运行完整的测试套件（51 个测试），确保无破坏性变更。

## 真实设备测试

1. **单集下载** — 验证基本功能
2. **批量下载** — 验证多集场景和统计功能
3. **搜索模式** — 验证自动化流程
4. **内容正确性** — 随机抽查视频文件

## 成功标准

- ✓ 所有单元测试通过（51/51）
- ✓ 真实设备测试通过（3 个场景）
- ✓ episode_number 提取成功率 > 80%
- ✓ 无内容错位（视频内容与文件名一致）
- ✓ 回退机制工作正常（提取失败时自动回退）
</verification>

<success_criteria>
1. 所有单元测试通过（51/51）
2. 真实设备测试通过（单集、批量、搜索模式）
3. episode_number 提取成功率 > 80%（或回退机制工作正常）
4. 无内容错位（随机抽查验证）
5. Phase 2 总结文档完整（包含测试结果和统计数据）
6. 所有提交已推送到 git
</success_criteria>

<output>
After completion, create `.planning/phases/02-hook-validation/02-03-SUMMARY.md`
</output>
