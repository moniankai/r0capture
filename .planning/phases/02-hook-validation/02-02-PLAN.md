---
phase: 02-hook-validation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/download_drama.py
autonomous: true
requirements: [HOOK-03]
must_haves:
  truths:
    - "当 episode_number 可用时，Python 端优先使用精确匹配选择数据"
    - "当 episode_number 不可用时，自动回退到时序选择（Phase 1 逻辑）"
    - "日志清晰显示使用了哪种匹配策略（精确匹配 vs 时序选择）"
  artifacts:
    - path: "scripts/download_drama.py"
      provides: "download_and_decrypt 函数增加精确匹配逻辑"
      contains: "matched_refs.*episode_number"
  key_links:
    - from: "download_and_decrypt"
      to: "VideoRef.episode_number"
      via: "列表推导式过滤"
      pattern: "r\\.episode_number == ep_num"
    - from: "download_and_decrypt"
      to: "AESKey.episode_number"
      via: "列表推导式过滤"
      pattern: "k\\.episode_number == ep_num"
---

<objective>
实现 Python 端的精确匹配逻辑，根据 episode_number 选择正确的 Hook 数据。

Purpose: 消除时序依赖，解决多集预加载场景下的数据竞争问题。当目标集和 preload 集的 Hook 同时触发时，能根据 episode_number 精确选择正确的数据。

Output: 
- 精确匹配逻辑（优先策略）
- 时序选择回退（兼容策略）
- 清晰的日志输出
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-stability/01-02-SUMMARY.md
@.planning/phases/02-hook-validation/02-CONTEXT.md
@.planning/phases/02-hook-validation/02-01-PLAN.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现精确匹配逻辑</name>
  <files>scripts/download_drama.py</files>
  <action>
修改 `download_and_decrypt()` 函数（第 400-600 行附近），增加基于 episode_number 的精确匹配逻辑：

1. **定位修改点**：
   找到 Phase 1 实现的时间戳过滤逻辑（第 450-480 行附近）：
   ```python
   FRESHNESS_THRESHOLD = 5.0
   now = time.time()
   
   # 过滤最近 5 秒内的数据
   recent_refs = [r for r in state.video_refs if now - r.timestamp < FRESHNESS_THRESHOLD]
   recent_keys = [k for k in state.aes_keys if now - k.timestamp < FRESHNESS_THRESHOLD]
   ```

2. **增加精确匹配逻辑**（在时间戳过滤之后）：
   ```python
   # 策略 1: 精确匹配 episode_number（优先）
   matched_refs = [r for r in recent_refs if r.episode_number == ep_num]
   matched_keys = [k for k in recent_keys if k.episode_number == ep_num]
   
   if matched_refs and matched_keys:
       # 精确匹配成功
       logger.info(
           f"[数据选择] ✓ 精确匹配 episode_number={ep_num} "
           f"(refs={len(matched_refs)}, keys={len(matched_keys)})"
       )
       
       # 选择最新的数据（按时间戳排序）
       vid_ref = sorted(matched_refs, key=lambda r: r.timestamp, reverse=True)[0]
       aes_key = sorted(matched_keys, key=lambda k: k.timestamp, reverse=True)[0]
       
       logger.debug(
           f"[数据选择] 选中 video_id={vid_ref.video_id[:8]}... "
           f"key={aes_key.key_hex[:8]}... (age={now - vid_ref.timestamp:.1f}s)"
       )
   
   elif matched_refs or matched_keys:
       # 部分匹配（仅 refs 或仅 keys 匹配）
       logger.warning(
           f"[数据选择] ⚠ 部分匹配 episode_number={ep_num} "
           f"(refs={len(matched_refs)}, keys={len(matched_keys)})，回退到时序选择"
       )
       matched_refs = []
       matched_keys = []
   
   else:
       # 无精确匹配，回退到时序选择
       logger.info(
           f"[数据选择] episode_number 不可用或无匹配，回退到时序选择 "
           f"(recent_refs={len(recent_refs)}, recent_keys={len(recent_keys)})"
       )
   
   # 策略 2: 时序选择（回退）
   if not matched_refs:
       if not recent_refs:
           logger.warning(
               f"[数据选择] ✗ 无新鲜数据 (超过 {FRESHNESS_THRESHOLD}s)，"
               f"total_refs={len(state.video_refs)}"
           )
           return False, "stale_data"
       
       # 选择最新的数据
       vid_ref = sorted(recent_refs, key=lambda r: r.timestamp, reverse=True)[0]
       logger.info(
           f"[数据选择] 时序选择 video_id={vid_ref.video_id[:8]}... "
           f"(age={now - vid_ref.timestamp:.1f}s)"
       )
   
   if not matched_keys:
       if not recent_keys:
           logger.warning(
               f"[数据选择] ✗ 无新鲜密钥 (超过 {FRESHNESS_THRESHOLD}s)，"
               f"total_keys={len(state.aes_keys)}"
           )
           return False, "stale_key"
       
       aes_key = sorted(recent_keys, key=lambda k: k.timestamp, reverse=True)[0]
       logger.info(
           f"[数据选择] 时序选择 key={aes_key.key_hex[:8]}... "
           f"(age={now - aes_key.timestamp:.1f}s)"
       )
   ```

3. **修改后续代码**：
   确保后续代码使用 `vid_ref` 和 `aes_key` 变量（而非 `_snap_refs[0]` 和 `_snap_keys[0]`）：
   ```python
   # 使用选中的数据
   video_id = vid_ref.video_id
   best_url_entry = state.best_video(quality, video_id)
   
   if not best_url_entry:
       logger.warning(f"[下载] 无可用 URL for video_id={video_id[:8]}...")
       return False, "no_url"
   
   # 下载和解密
   url = best_url_entry["url"]
   key_hex = aes_key.key_hex
   # ... 继续现有逻辑
   ```

**注意**：
- 保持 Phase 1 的时间戳过滤逻辑（5 秒阈值）
- 精确匹配失败时自动回退到时序选择
- 日志清晰显示使用的策略
- 不修改函数签名和返回值
  </action>
  <verify>
    <automated>
# 验证精确匹配逻辑存在
grep -n "matched_refs.*episode_number" scripts/download_drama.py
grep -n "精确匹配" scripts/download_drama.py

# 验证回退逻辑存在
grep -n "回退到时序选择" scripts/download_drama.py

# 运行现有测试
pytest tests/test_download_drama.py -v
    </automated>
  </verify>
  <done>
- `download_and_decrypt` 包含精确匹配逻辑（`matched_refs` 和 `matched_keys`）
- 回退到时序选择的逻辑存在
- 日志输出包含 "精确匹配" 和 "回退到时序选择"
- 所有现有测试通过（47/47）
  </done>
</task>

<task type="auto">
  <name>Task 2: 增加数据选择统计</name>
  <files>scripts/download_drama.py</files>
  <action>
在 `CaptureState` 类中增加数据选择统计，用于监控精确匹配的成功率：

1. **修改 CaptureState 类**（第 221-293 行）：
   ```python
   class CaptureState:
       def __init__(self) -> None:
           # ... 现有字段 ...
           
           # 数据选择统计（新增）
           self.match_stats = {
               "exact_match": 0,      # 精确匹配成功次数
               "fallback_timestamp": 0,  # 回退到时序选择次数
               "partial_match": 0,    # 部分匹配次数
           }
       
       def clear(self) -> None:
           with self.lock:
               # ... 现有清理逻辑 ...
               # 注意：不清理 match_stats（跨轮次累积）
   ```

2. **在 download_and_decrypt 中更新统计**：
   ```python
   # 精确匹配成功
   if matched_refs and matched_keys:
       state.match_stats["exact_match"] += 1
       logger.info(f"[数据选择] ✓ 精确匹配 episode_number={ep_num}")
   
   # 部分匹配
   elif matched_refs or matched_keys:
       state.match_stats["partial_match"] += 1
       logger.warning(f"[数据选择] ⚠ 部分匹配，回退到时序选择")
   
   # 时序选择
   else:
       state.match_stats["fallback_timestamp"] += 1
       logger.info(f"[数据选择] 回退到时序选择")
   ```

3. **在批量下载结束时输出统计**：
   在 `main()` 函数的批量下载循环结束后（第 1200 行附近）：
   ```python
   # 批量下载结束，输出统计
   if state.match_stats["exact_match"] + state.match_stats["fallback_timestamp"] > 0:
       total = sum(state.match_stats.values())
       exact_rate = state.match_stats["exact_match"] / total * 100 if total > 0 else 0
       
       logger.info(
           f"\n[统计] 数据选择策略分布:\n"
           f"  - 精确匹配: {state.match_stats['exact_match']} ({exact_rate:.1f}%)\n"
           f"  - 时序选择: {state.match_stats['fallback_timestamp']}\n"
           f"  - 部分匹配: {state.match_stats['partial_match']}"
       )
   ```

**注意**：
- 统计数据跨轮次累积（不在 `clear()` 中重置）
- 仅在批量下载模式下输出统计（单集下载无需统计）
- 统计信息用于监控 Hook 端 episode_number 提取的成功率
  </action>
  <verify>
    <automated>
# 验证统计字段存在
grep -n "match_stats" scripts/download_drama.py | head -5

# 验证统计更新逻辑
grep -n "exact_match.*+= 1" scripts/download_drama.py
grep -n "fallback_timestamp.*+= 1" scripts/download_drama.py

# 运行现有测试
pytest tests/test_download_drama.py -v
    </automated>
  </verify>
  <done>
- `CaptureState.match_stats` 字典存在
- `download_and_decrypt` 更新统计计数器
- 批量下载结束时输出统计信息
- 所有现有测试通过（47/47）
  </done>
</task>

<task type="auto">
  <name>Task 3: 增加单元测试</name>
  <files>tests/test_download_drama.py</files>
  <action>
在测试文件末尾增加 episode_number 匹配逻辑的单元测试：

```python
class EpisodeNumberMatchingTests(unittest.TestCase):
    """测试 episode_number 精确匹配逻辑"""
    
    def test_exact_match_success(self):
        """测试精确匹配成功场景"""
        from scripts.download_drama import VideoRef, AESKey
        import time
        
        now = time.time()
        
        # 模拟多集数据（目标集 + preload 集）
        refs = [
            VideoRef(video_id="vid001", duration=100, raw_data={}, 
                    timestamp=now, episode_number=5),
            VideoRef(video_id="vid002", duration=100, raw_data={}, 
                    timestamp=now, episode_number=6),  # preload
        ]
        
        keys = [
            AESKey(key_hex="key001", bits=128, timestamp=now, episode_number=5),
            AESKey(key_hex="key002", bits=128, timestamp=now, episode_number=6),
        ]
        
        # 精确匹配 episode_number=5
        ep_num = 5
        matched_refs = [r for r in refs if r.episode_number == ep_num]
        matched_keys = [k for k in keys if k.episode_number == ep_num]
        
        self.assertEqual(len(matched_refs), 1)
        self.assertEqual(matched_refs[0].video_id, "vid001")
        self.assertEqual(len(matched_keys), 1)
        self.assertEqual(matched_keys[0].key_hex, "key001")
    
    def test_fallback_to_timestamp(self):
        """测试回退到时序选择"""
        from scripts.download_drama import VideoRef, AESKey
        import time
        
        now = time.time()
        
        # 模拟无 episode_number 的数据
        refs = [
            VideoRef(video_id="vid001", duration=100, raw_data={}, 
                    timestamp=now - 1, episode_number=None),
            VideoRef(video_id="vid002", duration=100, raw_data={}, 
                    timestamp=now, episode_number=None),  # 最新
        ]
        
        keys = [
            AESKey(key_hex="key001", bits=128, timestamp=now - 1, episode_number=None),
            AESKey(key_hex="key002", bits=128, timestamp=now, episode_number=None),
        ]
        
        # 无精确匹配，回退到时序选择
        ep_num = 5
        matched_refs = [r for r in refs if r.episode_number == ep_num]
        matched_keys = [k for k in keys if k.episode_number == ep_num]
        
        self.assertEqual(len(matched_refs), 0)
        self.assertEqual(len(matched_keys), 0)
        
        # 时序选择：选择最新的数据
        latest_ref = sorted(refs, key=lambda r: r.timestamp, reverse=True)[0]
        latest_key = sorted(keys, key=lambda k: k.timestamp, reverse=True)[0]
        
        self.assertEqual(latest_ref.video_id, "vid002")
        self.assertEqual(latest_key.key_hex, "key002")
    
    def test_partial_match_fallback(self):
        """测试部分匹配回退场景"""
        from scripts.download_drama import VideoRef, AESKey
        import time
        
        now = time.time()
        
        # 模拟仅 refs 有 episode_number，keys 无
        refs = [
            VideoRef(video_id="vid001", duration=100, raw_data={}, 
                    timestamp=now, episode_number=5),
        ]
        
        keys = [
            AESKey(key_hex="key001", bits=128, timestamp=now, episode_number=None),
        ]
        
        # 部分匹配
        ep_num = 5
        matched_refs = [r for r in refs if r.episode_number == ep_num]
        matched_keys = [k for k in keys if k.episode_number == ep_num]
        
        self.assertEqual(len(matched_refs), 1)
        self.assertEqual(len(matched_keys), 0)  # 部分匹配，应回退
    
    def test_multiple_matches_select_latest(self):
        """测试多个匹配时选择最新数据"""
        from scripts.download_drama import VideoRef
        import time
        
        now = time.time()
        
        # 模拟同一集的多次捕获（重试场景）
        refs = [
            VideoRef(video_id="vid001", duration=100, raw_data={}, 
                    timestamp=now - 2, episode_number=5),
            VideoRef(video_id="vid002", duration=100, raw_data={}, 
                    timestamp=now, episode_number=5),  # 最新
            VideoRef(video_id="vid003", duration=100, raw_data={}, 
                    timestamp=now - 1, episode_number=5),
        ]
        
        ep_num = 5
        matched_refs = [r for r in refs if r.episode_number == ep_num]
        latest_ref = sorted(matched_refs, key=lambda r: r.timestamp, reverse=True)[0]
        
        self.assertEqual(latest_ref.video_id, "vid002")


if __name__ == "__main__":
    unittest.main()
```

**注意**：
- 测试覆盖精确匹配、回退、部分匹配、多匹配选择最新等场景
- 使用真实的数据类（`VideoRef`、`AESKey`）
- 测试逻辑与实现逻辑一致
  </action>
  <verify>
    <automated>
# 运行新增测试
pytest tests/test_download_drama.py::EpisodeNumberMatchingTests -v

# 运行所有测试
pytest tests/test_download_drama.py -v
    </automated>
  </verify>
  <done>
- `EpisodeNumberMatchingTests` 测试类存在
- 4 个新测试用例通过
- 所有现有测试通过（47 + 4 = 51 个测试）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook 数据 → 匹配逻辑 | episode_number 可能为 null 或错误值 |
| 精确匹配 → 回退逻辑 | 回退机制必须可靠，避免死锁 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | 精确匹配逻辑 | mitigate | 部分匹配时强制回退到时序选择，避免卡死 |
| T-02-05 | Information Disclosure | 统计信息 | accept | 统计数据仅用于调试，不包含敏感信息 |
| T-02-06 | Tampering | episode_number 值 | mitigate | 回退机制确保即使 episode_number 错误也能继续工作 |
</threat_model>

<verification>
## 单元测试

1. **精确匹配成功**：
   - 多集数据（目标集 + preload 集）
   - 精确匹配 episode_number=5
   - 验证选中正确的 video_id 和 key

2. **回退到时序选择**：
   - 无 episode_number 的数据
   - 验证选择最新的数据（按 timestamp）

3. **部分匹配回退**：
   - 仅 refs 有 episode_number，keys 无
   - 验证回退到时序选择

4. **多匹配选择最新**：
   - 同一集的多次捕获
   - 验证选择最新的数据

## 集成测试

1. **批量下载场景**：
   - 下载 3 集短剧
   - 验证统计信息输出
   - 验证日志包含 "精确匹配" 或 "回退到时序选择"

## 回归测试

运行 Phase 1 的 47 个测试 + 新增 4 个测试，确保无破坏性变更。
</verification>

<success_criteria>
1. `download_and_decrypt` 包含精确匹配逻辑（优先使用 episode_number）
2. 回退到时序选择的逻辑存在且可靠
3. `CaptureState.match_stats` 统计数据选择策略分布
4. 批量下载结束时输出统计信息
5. 新增 4 个单元测试通过
6. 所有现有测试通过（47 + 4 = 51 个测试）
7. 日志清晰显示使用的匹配策略
</success_criteria>

<output>
After completion, create `.planning/phases/02-hook-validation/02-02-SUMMARY.md`
</output>
